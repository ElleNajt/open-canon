// BWV 1087 Canon 3 - Variation: Midnight Masquerade

let maskedPiano = x => x
  .sound("triangle")
  .gain(0.13)
  .attack(0.001)
  .decay(0.3)
  .sustain(0.35)
  .release(0.8)
  .lpf(2200)
  .lpq(5)
  .room(0.6)
  .delay(0.3)
  .delaytime(0.125)
  .delayfeedback(0.35)

let velvetStrings = x => x
  .sound("sawtooth")
  .gain(0.11)
  .attack(0.4)
  .decay(0.5)
  .sustain(0.55)
  .release(1.4)
  .lpf(1100)
  .lpq(3)
  .room(0.55)

setcps(72/60/4)

// Track 1 - melody becomes mysterious, chromatic descent
$: note("e5 ds5 ~ d5 cs5 ~ c5 b4 ~ as4 a4 ~ gs4 g4 ~ e5")
  .slow(2)
  .apply(maskedPiano)
  .pan(0.35)
  .every(3, x => x.rev())
  .sometimes(x => x.add(12).gain(0.08))

// Track 2 - harmony shifts to minor mode, darker
$: note("<e3 g3 b3> ~ <c3 e3 g3> ~ <a2 c3 e3> ~ <b2 ds3 fs3> ~")
  .slow(4)
  .apply(velvetStrings)
  .pan(0.65)
  .jux(x => x.add(12).lpf(700))

// Track 3 - DROPPED pizzicato, ADDED: haunted music box, detuned
$: note("b5 ~ e6 ~ b5 ~ g5 ~ fs5 ~ e5 ~ ds5 ~ ~ ~")
  .slow(3)
  .sound("sine")
  .gain(0.1)
  .attack(0.001)
  .decay(0.15)
  .sustain(0.1)
  .release(0.5)
  .fmi(4)
  .fmh(5)
  .fmdecay(0.08)
  .detune(-15)
  .hpf(1800)
  .room(0.65)
  .pan(sine.range(0.25, 0.75).slow(5))

// Track 4 - heartbeat becomes irregular, anxious
$: note("e2 ~ ~ e2 ~ e2 e2 ~ ~ ~ e2 ~ e2 ~ ~ ~")
  .slow(2)
  .sound("sine")
  .gain(0.2)
  .attack(0.002)
  .decay(0.25)
  .sustain(0.4)
  .release(0.5)
  .lpf(280)
  .room(0.15)
  .pan(0.5)

// Track 5 - carousel becomes creaking, slowing down
$: note("e5 fs5 g5 ~ a5 ~ g5 fs5 ~ e5 ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("triangle")
  .gain(0.085)
  .attack(0.02)
  .decay(0.3)
  .sustain(0.2)
  .release(0.7)
  .lpf(1600)
  .lpq(6)
  .room(0.5)
  .pan(0.4)
  .sometimes(x => x.speed(0.95))

// Track 6 - spinning becomes stumbling waltz
$: note("e5 ~ ~ g5 ~ ~ fs5 ~ ~ e5 ~ ~ ds5 ~ ~ ~")
  .slow(3)
  .sound("sawtooth")
  .gain(0.08)
  .attack(0.08)
  .decay(0.2)
  .sustain(0.3)
  .release(0.6)
  .lpf(1400)
  .room(0.4)
  .pan(0.55)

// Track 7 - DROPPED tambourine, ADDED: distant bells tolling
$: note("e3 ~ ~ ~ ~ ~ ~ ~ e3 ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.12)
  .attack(0.01)
  .decay(1.5)
  .sustain(0.2)
  .release(2)
  .fmi(1.5)
  .fmh(2)
  .fmdecay(0.8)
  .room(0.85)
  .delay(0.5)
  .delaytime(0.5)
  .delayfeedback(0.45)
  .pan(0.5)

// Track 8 - shadow grows longer, in minor seconds
$: note("f4 ~ e4 ~ ~ ds4 ~ ~ d4 ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .late(0.0625)
  .sound("sawtooth")
  .gain(0.07)
  .attack(0.06)
  .decay(0.35)
  .sustain(0.35)
  .release(0.8)
  .lpf(1200)
  .room(0.5)
  .pan(0.72)

// Track 9 - footsteps now hesitant, stopping
$: note("e4 ~ ~ ~ g4 ~ ~ ~ e4 ~ ~ ~ ~ ~ ~ ~")
  .slow(2)
  .sound("triangle")
  .gain(0.085)
  .attack(0.001)
  .decay(0.05)
  .sustain(0.0)
  .release(0.12)
  .lpf(2200)
  .room(0.3)
  .pan(0.5)

// Track 10 - contrabass becomes drone, ominous
$: note("e1 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sawtooth")
  .gain(0.15)
  .attack(0.5)
  .decay(0.4)
  .sustain(0.8)
  .release(1.5)
  .lpf(400)
  .lpq(4)
  .room(0.45)
  .pan(0.5)

// Track 11 - fanfare becomes whispered secrets
$: note("<e4 g4 b4> ~ ~ ~ ~ ~ ~ ~ <c4 e4 g4> ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.09)
  .attack(0.5)
  .decay(0.6)
  .sustain(0.4)
  .release(2)
  .lpf(900)
  .room(0.7)
  .pan(0.5)

// Track 12 - fireworks become falling stars, descending
$: note("~ ~ ~ ~ ~ ~ ~ ~ b6 a6 g6 fs6 e6 ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.06)
  .attack(0.001)
  .decay(0.5)
  .sustain(0.15)
  .release(1.5)
  .fmi(6)
  .fmh(4)
  .fmdecay(0.3)
  .hpf(3500)
  .room(0.8)
  .delay(0.6)
  .delaytime(0.25)
  .delayfeedback(0.5)
  .pan(0.6)

// Track 13 - DROPPED marionette, ADDED: masked figure breathes
$: note("~ e4 ~ ~ ~ e4 ~ ~ ~ ~ e4 ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.07)
  .attack(0.3)
  .decay(0.5)
  .sustain(0.2)
  .release(0.8)
  .fmi(0.5)
  .fmh(0.5)
  .lpf(600)
  .room(0.4)
  .pan(0.45)

// Track 14 - applause becomes wind through curtains
$: note("e6 g6 e6 g6 e6 g6 e6 g6 e6 g6 e6 g6 ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.055)
  .attack(0.1)
  .decay(0.15)
  .sustain(0.1)
  .release(0.3)
  .fmi(0.3)
  .fmh(0.7)
  .hpf(3000)
  .room(0.6)
  .pan(rand.range(0.3, 0.7))
  .degradeBy(0.4)