// BWV 1087 Canon 3 - Variation: The Awakening After

let morningLight = x => x
  .sound("triangle")
  .gain(0.12)
  .attack(0.15)
  .decay(0.4)
  .sustain(0.4)
  .release(1.2)
  .lpf(2200)
  .lpq(1.5)
  .room(0.5)
  .delay(0.3)
  .delaytime(0.166)
  .delayfeedback(0.35)

let warmStrings = x => x
  .sound("sawtooth")
  .gain(0.09)
  .attack(0.3)
  .decay(0.5)
  .sustain(0.5)
  .release(1.5)
  .lpf(1400)
  .lpq(1)
  .room(0.45)

setcps(84/60/4)

// Track 1 - melody ascends now, hope returning in rising fourths
$: note("e4 ~ a4 ~ d5 ~ g5 ~ c6 ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .apply(morningLight)
  .pan(0.45)
  .every(4, x => x.add(7).gain(0.14))

// Track 2 - harmony opens up, major inflections, breathing
$: note("<e3 a3 cs4> ~ <d3 fs3 a3> ~ ~ <g3 b3 d4> ~ ~ <c4 e4 g4> ~ ~ ~ ~ ~")
  .slow(6)
  .apply(warmStrings)
  .pan(0.55)
  .jux(x => x.add(12).lpf(1800).gain(0.06))

// Track 3 - bells ring clearer now, celebratory fifths
$: note("e6 b6 ~ e6 b6 ~ ~ a6 e7 ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.09)
  .attack(0.002)
  .decay(0.6)
  .sustain(0.1)
  .release(1.5)
  .fmi(2)
  .fmh(3)
  .fmdecay(0.4)
  .hpf(2000)
  .room(0.6)
  .pan(0.6)

// Track 4 - heartbeat steadies, finds rhythm again
$: note("e2 ~ ~ e2 ~ ~ e2 ~ ~ e2 ~ ~ ~ ~ ~ ~")
  .slow(3)
  .sound("sine")
  .gain(0.16)
  .attack(0.008)
  .decay(0.3)
  .sustain(0.3)
  .release(0.6)
  .lpf(280)
  .room(0.2)
  .pan(0.5)

// Track 5 - DROPPED train whistle, ADDED: birdsong returns, playful
$: note("e6 gs6 b6 ~ e6 ~ a6 cs7 ~ ~ e7 ~ ~ ~ ~ ~")
  .slow(4)
  .sound("triangle")
  .gain(0.07)
  .attack(0.001)
  .decay(0.15)
  .sustain(0.0)
  .release(0.3)
  .hpf(3500)
  .room(0.55)
  .pan(rand.range(0.3, 0.7))
  .sometimes(x => x.add(5))

// Track 6 - dance finds its feet, syncopated joy
$: note("e4 ~ a4 e4 ~ d5 ~ a4 e4 ~ cs5 ~ ~ ~ ~ ~")
  .slow(2)
  .sound("square")
  .gain(0.07)
  .attack(0.01)
  .decay(0.2)
  .sustain(0.25)
  .release(0.5)
  .lpf(1600)
  .room(0.35)
  .pan(0.4)

// Track 7 - chimes become church bells, announcing
$: note("a2 ~ ~ e3 ~ ~ a3 ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.13)
  .attack(0.01)
  .decay(1.8)
  .sustain(0.15)
  .release(2.5)
  .fmi(1.5)
  .fmh(2.5)
  .fmdecay(1.2)
  .room(0.7)
  .delay(0.4)
  .delaytime(0.375)
  .delayfeedback(0.45)
  .pan(0.5)

// Track 8 - shadow transforms to light, ascending chromatically
$: note("e4 ~ f4 ~ fs4 ~ g4 ~ gs4 ~ a4 ~ as4 ~ b4 ~")
  .slow(8)
  .sound("sawtooth")
  .gain(0.07)
  .attack(0.08)
  .decay(0.4)
  .sustain(0.3)
  .release(1.0)
  .lpf(1200)
  .room(0.45)
  .pan(0.65)

// Track 9 - DROPPED clock, ADDED: children laughing (rhythmic bursts)
$: note("e5 e5 ~ a5 a5 ~ ~ d6 ~ e5 ~ ~ ~ ~ ~ ~")
  .slow(3)
  .sound("triangle")
  .gain(0.065)
  .attack(0.002)
  .decay(0.08)
  .sustain(0.0)
  .release(0.15)
  .hpf(1800)
  .room(0.4)
  .pan(0.35)
  .degradeBy(0.15)

// Track 10 - drone rises, opens into fifth, breathing deep
$: note("<e2 b2> ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sawtooth")
  .gain(0.11)
  .attack(0.8)
  .decay(0.6)
  .sustain(0.5)
  .release(2.0)
  .lpf(sine.range(300, 500).slow(12))
  .lpq(3)
  .room(0.4)
  .pan(0.5)

// Track 11 - proclamation grows confident, call and response
$: note("e4 ~ ~ a4 ~ ~ ~ d5 ~ ~ e4 ~ ~ a4 ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.09)
  .attack(0.1)
  .decay(0.5)
  .sustain(0.3)
  .release(1.5)
  .lpf(1100)
  .room(0.5)
  .pan(0.5)
  .off(0.25, x => x.add(7).gain(0.06))

// Track 12 - sparks become stars, steady constellations
$: note("e7 ~ ~ ~ b6 ~ ~ ~ e6 ~ ~ ~ a6 ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.06)
  .attack(0.001)
  .decay(1.0)
  .sustain(0.15)
  .release(2.5)
  .fmi(2)
  .fmh(5)
  .fmdecay(0.8)
  .hpf(3000)
  .room(0.7)
  .delay(0.5)
  .delaytime(0.25)
  .delayfeedback(0.4)
  .pan(0.55)

// Track 13 - clear voice emerges from static, pure now
$: note("a4 ~ cs5 ~ e5 ~ ~ a5 ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(4)
  .sound("sine")
  .gain(0.08)
  .attack(0.05)
  .decay(0.3)
  .sustain(0.4)
  .release(1.0)
  .lpf(2000)
  .room(0.5)
  .pan(0.48)

// Track 14 - DROPPED wind, ADDED: sunrise fanfare, brass-like
$: note("e3 ~ b3 ~ e4 ~ ~ gs4 ~ b4 ~ e5 ~ ~ ~ ~")
  .slow(8)
  .sound("square")
  .gain(0.07)
  .attack(0.02)
  .decay(0.6)
  .sustain(0.4)
  .release(1.0)
  .lpf(900)
  .lpq(2)
  .room(0.5)
  .pan(0.5)