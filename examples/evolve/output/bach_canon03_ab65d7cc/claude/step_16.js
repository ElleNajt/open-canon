// BWV 1087 Canon 3 - Variation: The Marionette's Lament

let brokenOrgan = x => x
  .sound("square")
  .gain(0.11)
  .attack(0.01)
  .decay(0.15)
  .sustain(0.5)
  .release(0.3)
  .lpf(1400)
  .lpq(5)
  .room(0.5)
  .delay(0.3)
  .delaytime(0.166)
  .delayfeedback(0.4)

let weepingStrings = x => x
  .sound("sawtooth")
  .gain(0.1)
  .attack(1.2)
  .decay(0.8)
  .sustain(0.6)
  .release(2.5)
  .lpf(750)
  .lpq(4)
  .room(0.7)

setcps(72/60/4)

// Track 1 - melody fragments, like a music box winding down
$: note("e5 ~ ~ f5 ~ ~ ~ ~ e5 ~ d5 ~ ~ ~ ~ ~")
  .slow(3)
  .apply(brokenOrgan)
  .pan(0.35)
  .every(4, x => x.add(12).gain(0.06))
  .sometimes(x => x.speed(0.5))

// Track 2 - descending minor seconds, the strings weep
$: note("<e3 b3> <eb3 a3> <d3 ab3> <db3 g3> <c3 gb3> <b2 f3>")
  .slow(6)
  .apply(weepingStrings)
  .pan(0.65)
  .jux(x => x.add(7).lpf(500))

// Track 3 - DROPPED toy piano, ADDED: child's xylophone, off-key
$: note("e6 f6 ~ ~ g6 ~ e6 ~ ~ fs6 ~ ~ ~ e6 ~ ~")
  .slow(2.5)
  .sound("sine")
  .gain(0.08)
  .attack(0.001)
  .decay(0.08)
  .sustain(0.0)
  .release(0.3)
  .fmi(15)
  .fmh(4)
  .fmdecay(0.01)
  .lpf(8000)
  .hpf(2500)
  .room(0.6)
  .pan(0.7)
  .degradeBy(0.15)

// Track 4 - bass becomes heartbeat, slow and irregular
$: note("e2 ~ ~ ~ ~ ~ e2 ~ ~ ~ ~ ~ ~ ~ e2 ~")
  .slow(4)
  .sound("sine")
  .gain(0.18)
  .attack(0.01)
  .decay(0.5)
  .sustain(0.2)
  .release(0.8)
  .lpf(200)
  .room(0.2)
  .pan(0.5)

// Track 5 - accordion becomes harmonium, breathing its last
$: note("<e4 g4 b4> ~ ~ <d4 f4 a4> ~ ~ <c4 e4 g4> ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("pulse")
  .gain(0.07)
  .attack(0.8)
  .decay(0.6)
  .sustain(0.5)
  .release(1.5)
  .lpf(sine.range(400, 900).slow(16))
  .tremolo(0.3)
  .tremolosync(3)
  .room(0.55)
  .pan(0.4)

// Track 6 - DROPPED diminished runs, ADDED: single falling tear
$: note("b5 ~ ~ ~ ~ a5 ~ ~ ~ ~ ~ gs5 ~ ~ ~ ~")
  .slow(8)
  .sound("triangle")
  .gain(0.09)
  .attack(0.002)
  .decay(0.6)
  .sustain(0.2)
  .release(2)
  .lpf(3000)
  .room(0.8)
  .delay(0.7)
  .delaytime(0.33)
  .delayfeedback(0.6)
  .pan(0.5)

// Track 7 - music box now plays in reverse, haunted memory
$: note("gs5 ~ ~ a5 ~ ~ ~ ~ ~ b5 ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.065)
  .attack(0.001)
  .decay(0.5)
  .sustain(0.05)
  .release(2)
  .fmi(6)
  .fmh(7)
  .fmdecay(0.2)
  .hpf(2500)
  .room(0.9)
  .pan(0.45)

// Track 8 - shadow becomes echo of itself, fading
$: note("e4 ~ ~ ~ ~ ~ ~ ~ ~ ~ bb3 ~ ~ ~ ~ ~")
  .slow(4)
  .late(0.5)
  .sound("sawtooth")
  .gain(0.055)
  .attack(0.1)
  .decay(0.4)
  .sustain(0.15)
  .release(1)
  .lpf(800)
  .room(0.7)
  .pan(0.3)

// Track 9 - DROPPED castanets, ADDED: distant clock tick
$: note("e6 ~ ~ ~ ~ ~ ~ ~ e6 ~ ~ ~ ~ ~ ~ ~")
  .slow(2)
  .sound("sine")
  .gain(0.06)
  .attack(0.001)
  .decay(0.01)
  .sustain(0.0)
  .release(0.05)
  .fmi(25)
  .fmh(12)
  .hpf(4000)
  .room(0.3)
  .pan(0.8)

// Track 10 - hurdy gurdy becomes cello drone, almost human
$: note("e2 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("sawtooth")
  .gain(0.1)
  .attack(2)
  .decay(0.5)
  .sustain(0.8)
  .release(4)
  .lpf(400)
  .lpq(6)
  .phaser(0.15)
  .phaserdepth(0.5)
  .room(0.65)
  .pan(0.5)

// Track 11 - calliope silenced, replaced by wind through strings
$: note("b4 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(12)
  .sound("sine")
  .gain(0.055)
  .attack(1.5)
  .decay(2)
  .sustain(0.3)
  .release(3)
  .fmi(0.5)
  .fmh(1.5)
  .hpf(1000)
  .lpf(3000)
  .room(0.85)
  .pan(sine.range(0.3, 0.7).slow(16))

// Track 12 - wine glass shatters slowly, frozen in time
$: note("e7 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("sine")
  .gain(0.05)
  .attack(0.8)
  .decay(2)
  .sustain(0.2)
  .release(6)
  .fmi(2)
  .fmh(3)
  .hpf(6000)
  .room(0.98)
  .delay(0.9)
  .delaytime(0.75)
  .delayfeedback(0.7)
  .pan(0.6)

// Track 13 - NEW: the marionette's strings snap, one by one
$: note("b6 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(12)
  .sound("sine")
  .gain(0.07)
  .attack(0.001)
  .decay(0.03)
  .sustain(0.0)
  .release(1.5)
  .fmi(30)
  .fmh(15)
  .fmdecay(0.005)
  .hpf(5000)
  .room(0.7)
  .pan(rand.range(0.2, 0.8))

// Track 14 - NEW: a final breath, the curtain falls
$: note("e3 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("triangle")
  .gain(0.08)
  .attack(0.3)
  .decay(1.5)
  .sustain(0.4)
  .release(3)
  .lpf(300)
  .room(0.5)
  .pan(0.5)