setcps(78/60/4)

$: note("c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~ g4 g4 f4 f4 e4 e4 d4 ~ g4 g4 f4 f4 e4 e4 d4 ~ c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~")
  .sound("triangle")
  .attack(0.01)
  .decay(0.14)
  .sustain(0.45)
  .release(0.4)
  .gain(0.6)
  .lpf(sine.range(400, 3600).slow(28))
  .lpq(5)
  .room(0.55)
  .size(0.72)
  .pan(0.5)
  .off(0.25, x => x.note(x => x + 7).gain(0.18).pan(0.28).lpf(1400))
  .off(0.5, x => x.note(x => x + 12).gain(0.08).pan(0.72).lpf(2000).delay(0.25).delaytime(0.375))
  .every(6, x => x.juxBy(0.45, x => x.rev().gain(0.35)))
  .every(9, x => x.transpose(5).lpf(2000))

$: note("<c2 f2 g2 eb2>*2")
  .sound("sawtooth")
  .lpf(sine.range(80, 700).slow(16))
  .lpq(7)
  .gain(0.42)
  .attack(0.04)
  .decay(0.3)
  .sustain(0.35)
  .release(0.6)
  .drive(0.4)
  .shape(0.22)
  .pan(sine.range(0.38, 0.62).slow(22))
  .every(5, x => x.note(x => x - 12).gain(0.52).lpf(180))
  .every(8, x => x.ply(2).gain(0.28).decay(0.1))

$: note("<c3 ~ f3 ~ g3 ~ eb3 ~>")
  .sound("sine")
  .attack(0.1)
  .decay(0.2)
  .sustain(0.6)
  .release(1.4)
  .gain(0.44)
  .fmi(sine.range(0.4, 3.5).slow(21))
  .fmh("<2 2 3 2>")
  .room(0.5)
  .size(0.68)
  .pan(sine.range(0.25, 0.75).slow(27))
  .every(4, x => x.fmh(4).gain(0.5))
  .every(7, x => x.transpose(5).fmi(6))

$: note("<c4 eb4 g4 bb4>*4")
  .sound("supersaw")
  .lpf(sine.range(200, 1400).slow(14))
  .lpq(2)
  .attack(0.4)
  .decay(0.65)
  .sustain(0.3)
  .release(2.0)
  .gain(0.11)
  .detune(28)
  .room(0.75)
  .size(0.88)
  .pan(sine.range(0.12, 0.88).slow(35))
  .delay(0.4)
  .delaytime(0.75)
  .delayfeedback(0.25)
  .every(8, x => x.fast(2).gain(0.07))
  .every(11, x => x.note("<c4 g4 bb4 c5>*4").detune(18))

$: note("c5 ~ eb5 ~ g5 ~ bb5 ~")
  .sound("square")
  .lpf(perlin.range(500, 3200).slow(9))
  .lpq(3)
  .attack(0.04)
  .decay(0.22)
  .sustain(0.22)
  .release(0.8)
  .gain(0.13)
  .fmi(2.2)
  .fmh("<2 3 2 4>")
  .pan(sine.range(0.52, 0.88).slow(15))
  .room(0.58)
  .size(0.78)
  .delay(0.4)
  .delaytime(0.375)
  .delayfeedback(0.28)
  .every(3, x => x.transpose(5))
  .every(7, x => x.rev())
  .degradeBy(0.12)

$: note("~ ~ ~ c4 ~ ~ ~ ~ ~ ~ ~ g3 ~ ~ ~ ~")
  .sound("piano")
  .attack(0.01)
  .decay(0.5)
  .sustain(0.18)
  .release(1.8)
  .gain(0.3)
  .room(0.65)
  .size(0.82)
  .pan(perlin.range(0.28, 0.68).slow(11))
  .lpf(3200)
  .every(6, x => x.transpose(5).gain(0.24))

$: s("bd ~ ~ bd ~ ~ bd ~")
  .gain(0.78)
  .room(0.1)
  .size(0.3)
  .sometimes(x => x.speed(0.88))
  .every(8, x => x.ply(2).gain(0.5))
  .every(12, x => x.euclid(5, 8).gain(0.72))

$: s("~ sn ~ ~ ~ sn ~ ~")
  .gain(0.54)
  .room(0.3)
  .size(0.48)
  .rarely(x => x.speed(1.15).gain(0.28))
  .sometimes(x => x.shape(0.16))
  .every(6, x => x.pan(sine.range(0.2, 0.8).slow(3)))
  .every(10, x => x.delay(0.35).delaytime(0.25).gain(0.4))

$: s("hh*8")
  .gain(perlin.range(0.08, 0.34).slow(3))
  .pan(sine.range(0.12, 0.88).slow(5))
  .lpf(perlin.range(2800, 14000).slow(6))
  .degradeBy(0.2)
  .every(4, x => x.fast(2).gain(0.12))
  .every(7, x => x.euclid(7, 8))

$: s("~ ~ oh ~ ~ ~ oh ~")
  .gain(perlin.range(0.14, 0.32).slow(7))
  .room(0.45)
  .size(0.6)
  .pan(perlin.range(0.32, 0.72).slow(9))
  .rarely(x => x.speed(0.78))
  .every(8, x => x.gain(0.42).delay(0.3).delaytime(0.5))

$: s("~ ~ ~ ~ cp ~ ~ ~")
  .gain(perlin.range(0.14, 0.3).slow(4))
  .room(0.62)
  .size(0.75)
  .pan(sine.range(0.33, 0.67).slow(6))
  .rarely(x => x.speed(0.75).room(0.85))

$: s("~ ~ ~ ~ ~ ~ rim ~")
  .gain(perlin.range(0.16, 0.28).slow(5))
  .pan(perlin.range(0.52, 0.82).slow(10))
  .room(0.32)
  .size(0.42)
  .degradeBy(0.2)
  .every(9, x => x.s("~ ~ ~ ~ ~ ~ cr ~").gain(0.22))