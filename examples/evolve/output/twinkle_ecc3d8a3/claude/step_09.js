setcps(78/60/4)

$: note("c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~ g4 g4 f4 f4 e4 e4 d4 ~ g4 g4 f4 f4 e4 e4 d4 ~ c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~")
  .sound("triangle")
  .attack(0.01)
  .decay(0.12)
  .sustain(0.4)
  .release(0.5)
  .gain(0.58)
  .lpf(sine.range(500, 3400).slow(32))
  .lpq(4)
  .room(0.52)
  .size(0.68)
  .pan(0.5)
  .off(0.25, x => x.note(x => x + 7).gain(0.16).pan(0.25).lpf(1600))
  .off(0.5, x => x.note(x => x + 12).gain(0.08).pan(0.75).lpf(2200).delay(0.2).delaytime(0.375))
  .every(6, x => x.juxBy(0.5, x => x.rev().gain(0.28)))
  .every(9, x => x.transpose(5).lpf(1800))

$: note("<c2 f2 g2 eb2>*2")
  .sound("sawtooth")
  .lpf(sine.range(60, 520).slow(14))
  .lpq(9)
  .gain(0.52)
  .attack(0.06)
  .decay(0.28)
  .sustain(0.3)
  .release(0.9)
  .drive(0.5)
  .shape(0.18)
  .pan(sine.range(0.35, 0.65).slow(21))
  .every(5, x => x.note(x => x - 12).gain(0.58).lpf(120))
  .every(8, x => x.ply(2).gain(0.22).decay(0.07))

$: note("<c3 ~ f3 ~ g3 ~ eb3 ~>")
  .sound("sine")
  .attack(0.18)
  .decay(0.22)
  .sustain(0.48)
  .release(2.2)
  .gain(0.42)
  .fmi(sine.range(0.1, 4.2).slow(26))
  .fmh("<2 3 4 2>")
  .room(0.65)
  .size(0.78)
  .pan(sine.range(0.18, 0.82).slow(33))
  .every(4, x => x.fmh(6).gain(0.48))
  .every(7, x => x.transpose(5).fmi(7))

$: note("<c4 eb4 g4 bb4>*4")
  .sound("supersaw")
  .lpf(sine.range(100, 780).slow(26))
  .lpq(2)
  .attack(0.8)
  .decay(1.0)
  .sustain(0.18)
  .release(3.2)
  .gain(0.09)
  .detune(32)
  .room(0.86)
  .size(0.96)
  .pan(sine.range(0.1, 0.9).slow(41))
  .delay(0.28)
  .delaytime(0.75)
  .delayfeedback(0.32)
  .every(8, x => x.fast(2).gain(0.06))
  .every(11, x => x.note("<c4 g4 bb4 c5>*4").detune(20))

$: note("c5 ~ eb5 ~ g5 ~ bb5 ~")
  .sound("square")
  .lpf(perlin.range(280, 2200).slow(16))
  .lpq(5)
  .attack(0.05)
  .decay(0.2)
  .sustain(0.12)
  .release(1.3)
  .gain(0.11)
  .fmi(2.6)
  .fmh("<3 4 2 5>")
  .pan(sine.range(0.45, 0.88).slow(23))
  .room(0.58)
  .size(0.8)
  .delay(0.28)
  .delaytime(0.375)
  .delayfeedback(0.3)
  .every(3, x => x.transpose(5))
  .every(7, x => x.rev())
  .degradeBy(0.22)

$: note("~ ~ ~ c4 ~ ~ ~ ~ ~ ~ ~ g3 ~ ~ ~ ~ ~ ~ ~ eb4 ~ ~ ~ ~")
  .sound("piano")
  .attack(0.01)
  .decay(0.8)
  .sustain(0.1)
  .release(3.0)
  .gain(0.28)
  .room(0.78)
  .size(0.9)
  .pan(perlin.range(0.2, 0.78).slow(18))
  .lpf(3000)
  .delay(0.22)
  .delaytime(0.75)
  .delayfeedback(0.18)
  .every(6, x => x.transpose(5).gain(0.22))
  .every(10, x => x.transpose(-7).lpf(1400).gain(0.32))

$: note("<bb2 g2 eb3 c3>")
  .sound("pulse")
  .lpf(perlin.range(220, 1400).slow(20))
  .lpq(8)
  .attack(0.12)
  .decay(0.2)
  .sustain(0.16)
  .release(0.7)
  .gain(0.1)
  .fmi(sine.range(1.0, 3.2).slow(23))
  .pan(sine.range(0.48, 0.82).slow(26))
  .room(0.38)
  .size(0.55)
  .degradeBy(0.3)
  .every(5, x => x.transpose(7).gain(0.13))
  .every(9, x => x.fast(2).degradeBy(0.5))

$: s("bd ~ ~ bd ~ ~ bd ~")
  .gain(0.84)
  .room(0.08)
  .size(0.28)
  .sometimes(x => x.speed(0.9))
  .every(8, x => x.ply(2).gain(0.5))
  .every(12, x => x.euclid(5, 8).gain(0.78))

$: s("~ sn ~ ~ ~ sn ~ ~")
  .gain(0.52)
  .room(0.28)
  .size(0.5)
  .rarely(x => x.speed(1.18).gain(0.26))
  .sometimes(x => x.shape(0.22))
  .every(6, x => x.pan(sine.range(0.18, 0.82).slow(3)))
  .every(10, x => x.delay(0.3).delaytime(0.25).gain(0.38))

$: s("hh*8")
  .gain(perlin.range(0.04, 0.22).slow(6))
  .pan(sine.range(0.08, 0.92).slow(8))
  .lpf(perlin.range(2800, 13000).slow(11))
  .degradeBy(0.25)
  .every(4, x => x.fast(2).gain(0.08))
  .every(7, x => x.euclid(7, 8))

$: s("~ ~ oh ~ ~ ~ oh ~")
  .gain(perlin.range(0.08, 0.24).slow(13))
  .room(0.44)
  .size(0.6)
  .pan(perlin.range(0.28, 0.72).slow(15))
  .rarely(x => x.speed(0.8))
  .every(8, x => x.gain(0.34).delay(0.22).delaytime(0.5))

$: s("~ ~ ~ ~ cp ~ ~ ~")
  .gain(perlin.range(0.09, 0.24).slow(7))
  .room(0.72)
  .size(0.84)
  .pan(sine.range(0.3, 0.7).slow(11))
  .rarely(x => x.speed(0.72).room(0.9))
  .every(7, x => x.euclid(3, 8).gain(0.2))

$: s("~ ~ ~ ~ ~ ~ rim ~")
  .gain(perlin.range(0.1, 0.22).slow(9))
  .pan(perlin.range(0.55, 0.85).slow(16))
  .room(0.28)
  .size(0.4)
  .degradeBy(0.32)
  .every(9, x => x.s("~ ~ ~ ~ ~ ~ cr ~").gain(0.16))