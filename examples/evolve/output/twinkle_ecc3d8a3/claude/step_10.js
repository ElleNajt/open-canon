setcps(78/60/4)

$: note("c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~ g4 g4 f4 f4 e4 e4 d4 ~ g4 g4 f4 f4 e4 e4 d4 ~ c4 c4 g4 g4 a4 a4 g4 ~ f4 f4 e4 e4 d4 d4 c4 ~")
  .sound("triangle")
  .attack(0.01)
  .decay(0.14)
  .sustain(0.38)
  .release(0.6)
  .gain(0.55)
  .lpf(sine.range(400, 3200).slow(40))
  .lpq(5)
  .room(0.55)
  .size(0.72)
  .pan(0.5)
  .off(0.25, x => x.note(x => x + 7).gain(0.14).pan(0.22).lpf(1400))
  .off(0.5, x => x.note(x => x + 12).gain(0.07).pan(0.78).lpf(2000).delay(0.18).delaytime(0.375))
  .every(6, x => x.juxBy(0.5, x => x.rev().gain(0.26)))
  .every(11, x => x.transpose(5).lpf(1600))

$: note("<c2 f2 g2 eb2>*2")
  .sound("sawtooth")
  .lpf(sine.range(55, 480).slow(16))
  .lpq(10)
  .gain(0.5)
  .attack(0.08)
  .decay(0.32)
  .sustain(0.28)
  .release(1.1)
  .drive(0.6)
  .shape(0.22)
  .pan(sine.range(0.32, 0.68).slow(23))
  .every(5, x => x.note(x => x - 12).gain(0.6).lpf(110))
  .every(8, x => x.ply(2).gain(0.2).decay(0.06))

$: note("<c3 ~ f3 ~ g3 ~ eb3 ~>")
  .sound("sine")
  .attack(0.22)
  .decay(0.28)
  .sustain(0.45)
  .release(2.6)
  .gain(0.4)
  .fmi(sine.range(0.2, 5.0).slow(30))
  .fmh("<2 3 4 3>")
  .room(0.7)
  .size(0.82)
  .pan(sine.range(0.15, 0.85).slow(37))
  .every(4, x => x.fmh(6).gain(0.46))
  .every(9, x => x.transpose(5).fmi(8))

$: note("<c4 eb4 g4 bb4>*4")
  .sound("supersaw")
  .lpf(sine.range(80, 700).slow(32))
  .lpq(2)
  .attack(1.0)
  .decay(1.2)
  .sustain(0.15)
  .release(3.8)
  .gain(0.08)
  .detune(28)
  .room(0.9)
  .size(0.98)
  .pan(sine.range(0.08, 0.92).slow(47))
  .delay(0.32)
  .delaytime(0.75)
  .delayfeedback(0.28)
  .every(8, x => x.fast(2).gain(0.05))
  .every(13, x => x.note("<c4 g4 bb4 c5>*4").detune(18))

$: note("c5 ~ eb5 ~ g5 ~ bb5 ~")
  .sound("square")
  .lpf(perlin.range(240, 2000).slow(20))
  .lpq(6)
  .attack(0.04)
  .decay(0.18)
  .sustain(0.1)
  .release(1.5)
  .gain(0.1)
  .fmi(2.8)
  .fmh("<3 4 2 5>")
  .pan(sine.range(0.42, 0.9).slow(27))
  .room(0.62)
  .size(0.84)
  .delay(0.3)
  .delaytime(0.375)
  .delayfeedback(0.28)
  .every(3, x => x.transpose(5))
  .every(8, x => x.rev())
  .degradeBy(0.28)

$: note("~ ~ ~ c4 ~ ~ ~ ~ ~ ~ ~ g3 ~ ~ ~ ~ ~ ~ ~ eb4 ~ ~ ~ ~")
  .sound("piano")
  .attack(0.01)
  .decay(1.0)
  .sustain(0.08)
  .release(3.5)
  .gain(0.3)
  .room(0.82)
  .size(0.92)
  .pan(perlin.range(0.18, 0.82).slow(22))
  .lpf(2800)
  .delay(0.24)
  .delaytime(0.75)
  .delayfeedback(0.2)
  .every(5, x => x.transpose(5).gain(0.2))
  .every(11, x => x.transpose(-7).lpf(1200).gain(0.34))

$: note("<bb2 g2 eb3 c3>")
  .sound("pulse")
  .lpf(perlin.range(180, 1200).slow(24))
  .lpq(9)
  .attack(0.14)
  .decay(0.24)
  .sustain(0.14)
  .release(0.8)
  .gain(0.09)
  .fmi(sine.range(1.2, 3.8).slow(28))
  .pan(sine.range(0.44, 0.86).slow(31))
  .room(0.42)
  .size(0.58)
  .degradeBy(0.35)
  .every(5, x => x.transpose(7).gain(0.12))
  .every(10, x => x.fast(2).degradeBy(0.55))

$: s("bd ~ ~ bd ~ ~ bd ~")
  .gain(0.86)
  .room(0.06)
  .size(0.24)
  .lpf(perlin.range(180, 900).slow(20))
  .sometimes(x => x.speed(0.88))
  .every(8, x => x.ply(2).gain(0.48))
  .every(14, x => x.euclid(5, 8).gain(0.76))

$: s("~ sn ~ ~ ~ sn ~ ~")
  .gain(0.5)
  .room(0.32)
  .size(0.52)
  .pan(perlin.range(0.38, 0.62).slow(17))
  .rarely(x => x.speed(1.22).gain(0.24))
  .sometimes(x => x.shape(0.26))
  .every(6, x => x.pan(sine.range(0.15, 0.85).slow(3)))
  .every(12, x => x.delay(0.32).delaytime(0.25).gain(0.36))

$: s("hh*8")
  .gain(perlin.range(0.03, 0.2).slow(7))
  .pan(sine.range(0.06, 0.94).slow(10))
  .lpf(perlin.range(2400, 12000).slow(13))
  .degradeBy(0.3)
  .every(4, x => x.fast(2).gain(0.07))
  .every(9, x => x.euclid(7, 8))

$: s("~ ~ oh ~ ~ ~ oh ~")
  .gain(perlin.range(0.07, 0.22).slow(15))
  .room(0.48)
  .size(0.64)
  .pan(perlin.range(0.25, 0.75).slow(18))
  .rarely(x => x.speed(0.78))
  .every(10, x => x.gain(0.32).delay(0.24).delaytime(0.5))

$: s("~ ~ ~ ~ cp ~ ~ ~")
  .gain(perlin.range(0.08, 0.22).slow(9))
  .room(0.76)
  .size(0.88)
  .pan(sine.range(0.28, 0.72).slow(13))
  .rarely(x => x.speed(0.68).room(0.95))
  .every(8, x => x.euclid(3, 8).gain(0.18))

$: s("~ ~ ~ ~ ~ ~ rim ~")
  .gain(perlin.range(0.08, 0.2).slow(11))
  .pan(perlin.range(0.52, 0.88).slow(19))
  .room(0.3)
  .size(0.44)
  .degradeBy(0.38)
  .every(11, x => x.s("~ ~ ~ ~ ~ ~ cr ~").gain(0.14))