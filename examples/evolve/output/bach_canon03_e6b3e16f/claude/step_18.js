setcps(112/60/4)

// Baroque style voices
let upperVoice = x => x
  .sound("triangle")
  .gain(0.42)
  .attack(0.03)
  .decay(0.18)
  .sustain(0.15)
  .release(0.12)
  .lpf(2200)
  .room(0.3)
  .size(0.55)

let lowerVoice = x => x
  .sound("square")
  .gain(0.35)
  .attack(0.01)
  .decay(0.15)
  .sustain(0.18)
  .release(0.1)
  .lpf(1200)
  .room(0.22)
  .size(0.5)

let bassVoice = x => x
  .sound("sine")
  .gain(0.52)
  .attack(0.06)
  .decay(0.25)
  .sustain(0.35)
  .release(0.2)
  .lpf(700)
  .room(0.15)

let innerVoice = x => x
  .sound("sawtooth")
  .gain(0.22)
  .attack(0.08)
  .decay(0.3)
  .sustain(0.2)
  .release(0.18)
  .lpf(900)
  .lpq(4)
  .room(0.35)
  .size(0.6)

// Track 1 - upper voice, slightly wider pitch drift, more open room
$: note("~!40 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!7 b5 ~!7 a5 ~!7 g5 ~!7 d6 ~!7 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!7 b5 ~!7 a5 ~!7 g5 ~!7 d6 ~!7 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!15")
  .slow(208/8/4)
  .apply(upperVoice)
  .pan(0.22)
  .detune(sine.range(-10, 10).slow(317))
  .fmi(sine.range(0.0, 1.8).slow(89))
  .fmh(sine.range(2.0, 6.0).slow(233))
  .fmenv(sine.range(0.0, 0.55).slow(103))
  .fmdecay(0.28)
  .penv(sine.range(-0.2, 1.1).slow(179))
  .pdecay(0.32)
  .trem(sine.range(0.0, 0.45).slow(61))
  .tremolosync(perlin.range(2, 6).slow(97))
  .tremoloshape("sine")
  .room(sine.range(0.62, 0.96).slow(71))
  .size(sine.range(0.7, 0.98).slow(53))
  .roomlp(sine.range(600, 4200).slow(143))
  .juxBy(0.28, x => x.late(0.5).gain(0.05).room(0.99).size(0.99).lpf(220).pan(0.88))

// Track 2 - lower voice, vowel deepened, leslie more pronounced
$: note("~!8 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!7 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!7 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!15")
  .slow(208/8/4)
  .apply(lowerVoice)
  .pan(0.78)
  .vowel(sine.range(0, 1).slow(331))
  .lpf(sine.range(180, 1800).slow(97))
  .lpq(sine.range(2, 8).slow(131))
  .hpf(sine.range(28, 180).slow(199))
  .phaser(sine.range(0.05, 0.9).slow(113))
  .phaserdepth(sine.range(0.15, 0.75).slow(157))
  .leslie(sine.range(0.2, 1.2).slow(89))
  .lrate(sine.range(0.03, 2.4).slow(157))
  .lsize(sine.range(0.08, 0.6).slow(113))
  .shape(sine.range(0, 0.13).slow(211))
  .delay(sine.range(0, 0.22).slow(173))
  .delaytime(0.375)
  .delayfeedback(0.35)
  .off(0.375, x => x.gain(0.05).pan(0.5).lpf(360).room(0.88).size(0.9))

// Track 3 - inner voice, ring mod more present, panning widens
$: note("<b3 c4 d4 b3 a3 g3 e3 fs3>")
  .slow(41)
  .apply(innerVoice)
  .pan(sine.range(0.12, 0.88).slow(83))
  .lpf(sine.range(180, 2200).slow(67))
  .lpq(sine.range(1.5, 9).slow(79))
  .fmi(sine.range(0.0, 1.6).slow(163))
  .fmh(sine.range(1.5, 6.0).slow(241))
  .penv(sine.range(-0.5, 1.1).slow(107))
  .pdecay(0.4)
  .ring(sine.range(0, 0.28).slow(137))
  .ringf(sine.range(110, 680).slow(97))
  .ringdf(sine.range(0, 0.15).slow(167))
  .phaser(sine.range(0.03, 0.85).slow(191))
  .phaserdepth(sine.range(0, 0.75).slow(139))
  .room(sine.range(0.5, 0.92).slow(109))
  .size(sine.range(0.55, 0.9).slow(151))
  .gain(0.16)

// Track 4 - bass, warmer triode, countermelody more distinct
$: note("<g2 g2 d3 a2 c3 d3 e2 b2>")
  .slow(29)
  .apply(bassVoice)
  .pan(0.5)
  .lpf(sine.range(60, 1100).slow(59))
  .lpq(sine.range(1.5, 6).slow(83))
  .penv(sine.range(-0.6, 2.8).slow(41))
  .pdecay(0.5)
  .shape(sine.range(0, 0.22).slow(127))
  .triode(sine.range(0, 1.6).slow(71))
  .coarse(perlin.range(1, 2.5).slow(53))
  .off(0.5, x => x.note("<d2 g2 a2 e2 c2 g2 b2 fs2>")
    .gain(0.14)
    .lpf(sine.range(60, 560).slow(79))
    .room(0.88)
    .size(0.95)
    .pan(0.42)
    .fmi(sine.range(0.0, 1.1).slow(107))
    .fmh(sine.range(1.5, 4.0).slow(149))
    .attack(0.55)
    .decay(1.0)
    .sustain(0.12)
    .release(0.85)
    .delay(sine.range(0, 0.28).slow(197))
    .delaytime(0.75)
    .delayfeedback(0.36))