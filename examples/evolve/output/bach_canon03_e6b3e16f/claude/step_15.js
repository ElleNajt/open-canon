setcps(112/60/4)

// Baroque style voices
let upperVoice = x => x
  .sound("triangle")
  .gain(0.42)
  .attack(0.03)
  .decay(0.18)
  .sustain(0.15)
  .release(0.12)
  .lpf(2200)
  .room(0.3)
  .size(0.55)

let lowerVoice = x => x
  .sound("square")
  .gain(0.35)
  .attack(0.01)
  .decay(0.15)
  .sustain(0.18)
  .release(0.1)
  .lpf(1200)
  .room(0.22)
  .size(0.5)

let bassVoice = x => x
  .sound("sine")
  .gain(0.52)
  .attack(0.06)
  .decay(0.25)
  .sustain(0.35)
  .release(0.2)
  .lpf(700)
  .room(0.15)

let innerVoice = x => x
  .sound("sawtooth")
  .gain(0.22)
  .attack(0.08)
  .decay(0.3)
  .sustain(0.2)
  .release(0.18)
  .lpf(900)
  .lpq(4)
  .room(0.35)
  .size(0.6)

// Track 1 - upper voice, now with gentle pitch envelope and wider cathedral reverb
$: note("~!40 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!7 b5 ~!7 a5 ~!7 g5 ~!7 d6 ~!7 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!7 b5 ~!7 a5 ~!7 g5 ~!7 d6 ~!7 d5 ~!7 e5 ~!7 gb5 ~!7 g5 ~!15")
  .slow(208/8/4)
  .apply(upperVoice)
  .pan(0.22)
  .detune(sine.range(-12, 12).slow(317))
  .fmi(sine.range(0.0, 2.2).slow(89))
  .fmh(sine.range(1.5, 6).slow(233))
  .fmenv(sine.range(0.0, 0.7).slow(103))
  .fmdecay(0.28)
  .penv(sine.range(-0.3, 0.8).slow(179))
  .pdecay(0.35)
  .room(sine.range(0.55, 0.97).slow(71))
  .size(sine.range(0.6, 0.95).slow(53))
  .roomlp(sine.range(900, 3500).slow(143))
  .echo(3, 0.75, 0.28)
  .juxBy(0.35, x => x.late(0.375).gain(0.06).room(0.99).size(0.98).lpf(380).pan(0.88))

// Track 2 - lower voice, vowel more languid, leslie gentler, hpf breathes slowly
$: note("~!8 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!7 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!7 g5 ~!7 gb5 ~!7 e5 ~!7 d5 ~!7 b4 ~!7 c5 ~!7 d5 ~!7 g4 ~!15")
  .slow(208/8/4)
  .apply(lowerVoice)
  .pan(0.78)
  .vowel(sine.range(0, 1).slow(277))
  .lpf(sine.range(280, 1600).slow(97))
  .lpq(sine.range(1, 6).slow(131))
  .hpf(sine.range(35, 200).slow(199))
  .leslie(sine.range(0, 0.9).slow(89))
  .lrate(sine.range(0.05, 1.8).slow(157))
  .lsize(sine.range(0.08, 0.42).slow(113))
  .shape(sine.range(0, 0.08).slow(211))
  .off(0.25, x => x.gain(0.06).pan(0.5).lpf(550).room(0.8).size(0.8).delay(0.15).delaytime(0.33).delayfeedback(0.3))

// Track 3 - inner voice, phaser slower and more hypnotic, now with subtle fm breath
$: note("<b3 c4 d4 b3 a3 g3 e3 fs3>")
  .slow(29)
  .apply(innerVoice)
  .pan(sine.range(0.08, 0.92).slow(67))
  .lpf(sine.range(200, 1600).slow(53))
  .lpq(sine.range(1, 8).slow(79))
  .fmi(sine.range(0.0, 1.2).slow(163))
  .fmh(sine.range(2.0, 5.0).slow(241))
  .ring(sine.range(0, 0.18).slow(137))
  .ringf(sine.range(160, 520).slow(97))
  .ringdf(sine.range(0, 0.1).slow(167))
  .phaser(sine.range(0.05, 1.2).slow(127))
  .phaserdepth(sine.range(0, 0.6).slow(139))
  .phasersweep(sine.range(250, 1800).slow(199))
  .echo(2, 0.667, 0.22)
  .gain(0.13)

// Track 4 - bass, deeper shape drift, off-voice gains slow delay bloom
$: note("<g2 g2 d3 a2 c3 d3 e2 b2>")
  .slow(19)
  .apply(bassVoice)
  .pan(0.5)
  .lpf(sine.range(70, 900).slow(59))
  .lpq(sine.range(1, 7).slow(83))
  .penv(sine.range(-0.8, 2.2).slow(37))
  .pdecay(0.55)
  .shape(sine.range(0, 0.18).slow(127))
  .triode(sine.range(0, 1.1).slow(71))
  .off(0.5, x => x.note("<d2 g2 a2 e2 c2 g2 b2 fs2>")
    .gain(0.16)
    .lpf(sine.range(120, 460).slow(79))
    .room(0.82)
    .size(0.92)
    .pan(0.42)
    .fmi(sine.range(0.05, 0.8).slow(107))
    .fmh(sine.range(1.0, 3.0).slow(149))
    .attack(0.35)
    .decay(0.7)
    .sustain(0.22)
    .release(0.5)
    .delay(sine.range(0, 0.35).slow(173))
    .delaytime(0.5)
    .delayfeedback(0.45)
    .tremolo(sine.range(0, 0.35).slow(101))
    .tremolosync(sine.range(0.5, 2.0).slow(131)))