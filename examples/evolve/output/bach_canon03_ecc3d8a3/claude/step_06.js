setcps(100/60/4)

let upperVoice = x => x
  .sound("triangle")
  .gain(0.42)
  .attack(0.03)
  .decay(0.28)
  .sustain(0.14)
  .release(0.22)
  .lpf(sine.range(1600, 3400).slow(37))
  .room(0.38)
  .size(0.7)

let lowerVoice = x => x
  .sound("square")
  .gain(0.28)
  .attack(0.03)
  .decay(0.22)
  .sustain(0.14)
  .release(0.16)
  .lpf(sine.range(700, 1200).slow(41))
  .lpq(2)
  .room(0.32)
  .size(0.65)

let bassVoice = x => x
  .sound("sine")
  .gain(0.48)
  .attack(0.08)
  .decay(0.5)
  .sustain(0.32)
  .release(0.4)
  .lpf(380)
  .room(0.22)
  .size(0.58)

let innerVoice = x => x
  .sound("triangle")
  .gain(0.22)
  .attack(0.06)
  .decay(0.35)
  .sustain(0.18)
  .release(0.25)
  .lpf(sine.range(1200, 2400).slow(29))
  .room(0.38)
  .size(0.7)

// Upper voice — subject with canon echo and pitch envelope bloom
$: note("~!32 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 eb5 ~!11 d5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 eb6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!23")
  .slow(240/8/4)
  .apply(upperVoice)
  .penv(3).pattack(0.04).pdecay(0.18)
  .off(0.125, x => x.transpose(7).gain(0.13).lpf(2000))
  .off(0.5, x => x.transpose(-5).gain(0.08).sound("sine").lpf(1400).room(0.6))
  .pan(sine.range(0.18, 0.44).slow(31))
  .orbit(1)

// Middle voice — answer at fifth below, leslie warmth replacing phaser
$: note("~!12 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!11 c5 ~!11 bb4 ~!11 a4 ~!11 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!23")
  .slow(240/8/4)
  .apply(lowerVoice)
  .leslie(sine.range(0.1, 0.6).slow(19))
  .lrate(0.8)
  .pan(sine.range(0.56, 0.82).slow(23))
  .orbit(2)

// Inner voice — harmonic filler, now with vowel color drift
$: note("~!20 bb4 ~!23 a4 ~!23 g4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 f4 ~!19")
  .slow(240/8/4)
  .apply(innerVoice)
  .vowel(saw.range(0,1).slow(43))
  .trem(0.22)
  .tremolosync(12)
  .pan(sine.range(0.38, 0.62).slow(19))
  .orbit(3)

// Bass — walking harmonic motion, dorian flavor, now with ring mod shimmer
$: note("<g2 g2 c3 c3 d3 d3 eb3 eb3 d3 d3 bb2 bb2 c3 c3 d3 d3>")
  .slow(20)
  .apply(bassVoice)
  .ring(sine.range(0, 0.18).slow(53))
  .ringf(sine.range(60, 180).slow(37))
  .off(0.25, x => x.transpose(12).gain(0.09).sound("triangle").lpf(500).room(0.4))
  .pan(sine.range(0.44, 0.56).slow(61))
  .orbit(4)

// Shimmer — high register, golden ratio delay, now with bitcrush texture
$: note("~!32 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 bb6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!23")
  .slow(240/8/4)
  .sound("sine")
  .gain(0.09)
  .attack(0.14)
  .decay(0.7)
  .sustain(0.05)
  .release(0.65)
  .crush(sine.range(10, 16).slow(29))
  .lpf(5000)
  .delay(0.55)
  .delaytime(0.618)
  .delayfeedback(0.42)
  .room(0.75)
  .size(0.92)
  .pan(sine.range(0.25, 0.75).slow(43))
  .orbit(5)

// Breath pad — slow FM, evolving vowel, now with HP sweep for air
$: note("<g3 bb3 d4 f4 g3 eb4 d4 c4>")
  .slow(32)
  .sound("triangle")
  .gain(sine.range(0.05, 0.14).slow(47))
  .attack(1.4)
  .decay(1.0)
  .sustain(0.45)
  .release(1.8)
  .fmi(sine.range(0.1, 0.8).slow(71))
  .fmh(2)
  .lpf(sine.range(280, 820).slow(53))
  .hpf(sine.range(40, 180).slow(89))
  .lpq(1.2)
  .room(0.62)
  .size(0.88)
  .pan(sine.range(0.4, 0.6).slow(79))
  .orbit(6)

// Counter-melody — supersaw, now with bpf resonance sweep
$: note("~!48 eb4 ~!23 d4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 c4 ~!11")
  .slow(240/8/4)
  .sound("supersaw")
  .gain(0.11)
  .attack(0.12)
  .decay(0.5)
  .sustain(0.18)
  .release(0.4)
  .bpf(sine.range(500, 1200).slow(31))
  .bpq(sine.range(1.5, 4).slow(23))
  .detune(8)
  .room(0.5)
  .size(0.76)
  .pan(sine.range(0.3, 0.7).slow(17))
  .orbit(7)

// Slow cantus — descending dorian anchor, now jux reverses for echo shadow
$: note("<d5 c5 bb4 a4 g4 f4 eb4 d4>")
  .slow(96)
  .sound("triangle")
  .gain(sine.range(0.08, 0.18).slow(83))
  .attack(2.2)
  .decay(1.5)
  .sustain(0.55)
  .release(2.8)
  .fmi(sine.range(0.05, 0.35).slow(97))
  .fmh(3)
  .lpf(sine.range(600, 1400).slow(67))
  .lpq(1.0)
  .juxBy(0.4, x => x.rev().room(0.85).size(0.98).gain(0.06))
  .room(0.72)
  .size(0.94)
  .orbit(8)

// New: sparse high pings — sawtooth, euclidean, very brief, entering mid-piece
$: note("~!64 <d6 g6 bb5 a6 f6 eb6 c6 g6>")
  .slow(240/8/4)
  .euclid(3, 8)
  .sound("sawtooth")
  .gain(0.07)
  .attack(0.001)
  .decay(0.12)
  .sustain(0.0)
  .release(0.18)
  .lpf(4000)
  .lpq(6)
  .delay(0.4)
  .delaytime(0.382)
  .delayfeedback(0.35)
  .room(0.6)
  .size(0.8)
  .pan(perlin.range(0.2, 0.8).slow(7))
  .orbit(9)