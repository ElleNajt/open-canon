setcps(92/60/4)

let upperVoice = x => x
  .sound("triangle")
  .gain(0.38)
  .attack(0.04)
  .decay(0.32)
  .sustain(0.16)
  .release(0.28)
  .lpf(sine.range(1400, 3200).slow(41))
  .room(0.42)
  .size(0.72)

let lowerVoice = x => x
  .sound("square")
  .gain(0.20)
  .attack(0.04)
  .decay(0.24)
  .sustain(0.12)
  .release(0.18)
  .lpf(sine.range(600, 1100).slow(47))
  .lpq(2.2)
  .room(0.35)
  .size(0.68)

let bassVoice = x => x
  .sound("sine")
  .gain(0.42)
  .attack(0.10)
  .decay(0.55)
  .sustain(0.30)
  .release(0.45)
  .lpf(420)
  .room(0.20)
  .size(0.55)

let innerVoice = x => x
  .sound("triangle")
  .gain(0.18)
  .attack(0.07)
  .decay(0.38)
  .sustain(0.16)
  .release(0.28)
  .lpf(sine.range(1100, 2200).slow(33))
  .room(0.40)
  .size(0.72)

// Upper voice — subject with canon echo
$: note("~!32 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 eb5 ~!11 d5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 eb6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!23")
  .slow(240/8/4)
  .apply(upperVoice)
  .penv(2.5).pattack(0.05).pdecay(0.22)
  .off(0.125, x => x.transpose(7).gain(0.10).lpf(1800))
  .off(0.5, x => x.transpose(-5).gain(0.06).sound("sine").lpf(1200).room(0.65))
  .sometimes(x => x.detune(perlin.range(-8, 8)))
  .pan(sine.range(0.16, 0.42).slow(37))
  .orbit(1)

// Middle voice — answer at fifth below
$: note("~!12 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!11 c5 ~!11 bb4 ~!11 a4 ~!11 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!23")
  .slow(240/8/4)
  .apply(lowerVoice)
  .leslie(sine.range(0.08, 0.55).slow(23))
  .lrate(sine.range(0.4, 1.0).slow(47))
  .rarely(x => x.vowel("<a e i o>").gain(0.26))
  .pan(sine.range(0.58, 0.84).slow(29))
  .orbit(2)

// Inner voice — harmonic filler
$: note("~!20 bb4 ~!23 a4 ~!23 g4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 f4 ~!19")
  .slow(240/8/4)
  .apply(innerVoice)
  .trem(sine.range(0.08, 0.36).slow(71))
  .tremolosync(sine.range(8, 20).slow(43))
  .tremoloshape("sine")
  .off(0.333, x => x.gain(0.05).transpose(12).sound("sine").lpf(1600).room(0.72))
  .pan(sine.range(0.36, 0.64).slow(23))
  .orbit(3)

// Bass — walking harmonic motion
$: note("<g2 g2 c3 c3 d3 d3 eb3 eb3 d3 d3 bb2 bb2 c3 c3 d3 d3>")
  .slow(22)
  .apply(bassVoice)
  .penv(1.2).pattack(0.08).pdecay(0.9)
  .off(0.25, x => x.transpose(12).gain(0.07).sound("triangle").lpf(480).room(0.38))
  .off(0.5, x => x.transpose(-12).gain(0.18).sound("sine").lpf(110).room(0.14))
  .shape(sine.range(0, 0.16).slow(83))
  .pan(sine.range(0.42, 0.58).slow(67))
  .orbit(4)

// Shimmer — high sine, golden ratio delay
$: note("~!32 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 bb6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!23")
  .slow(240/8/4)
  .sound("sine")
  .gain(sine.range(0.03, 0.18).slow(59))
  .attack(0.20)
  .decay(1.0)
  .sustain(0.04)
  .release(0.9)
  .fmi(sine.range(0.0, 0.7).slow(89))
  .fmh(sine.range(2.5, 5.5).slow(127))
  .phaser(sine.range(0.2, 1.8).slow(53))
  .phaserdepth(sine.range(0.2, 1.2).slow(79))
  .phasersweep(sine.range(900, 4400).slow(107))
  .lpf(5000)
  .delay(0.50)
  .delaytime(0.618)
  .delayfeedback(0.38)
  .room(0.80)
  .size(0.94)
  .pan(sine.range(0.22, 0.78).slow(47))
  .orbit(5)

// Breath pad — slow FM, pulse
$: note("<g3 bb3 d4 f4 g3 eb4 d4 c4>")
  .slow(40)
  .sound("pulse")
  .gain(sine.range(0.02, 0.13).slow(53))
  .attack(2.6)
  .decay(1.8)
  .sustain(0.28)
  .release(3.2)
  .fmi(sine.range(0.1, 2.0).slow(97))
  .fmh(sine.range(1.5, 5.5).slow(137))
  .lpf(sine.range(200, 1400).slow(67))
  .hpf(sine.range(50, 160).slow(109))
  .lpq(1.4)
  .ring(sine.range(0.0, 0.14).slow(89))
  .ringf(sine.range(80, 320).slow(71))
  .room(0.72)
  .size(0.93)
  .pan(sine.range(0.33, 0.67).slow(83))
  .orbit(6)

// Slow cantus — descending dorian anchor
$: note("<d5 c5 bb4 a4 g4 f4 eb4 d4>")
  .slow(112)
  .sound("triangle")
  .gain(sine.range(0.10, 0.28).slow(89))
  .attack(2.8)
  .decay(2.0)
  .sustain(0.48)
  .release(3.5)
  .fmi(sine.range(0.05, 0.9).slow(113))
  .fmh(sine.range(2.0, 6.5).slow(149))
  .lpf(sine.range(900, 4000).slow(79))
  .lpq(1.2)
  .triode(sine.range(0.0, 0.18).slow(127))
  .juxBy(0.40, x => x.rev().room(0.92).size(0.98).gain(0.05).transpose(7).lpf(1200))
  .room(0.76)
  .size(0.96)
  .orbit(8)

// Sparse high pings — sawtooth, euclidean
$: note("<d6 g6 bb5 a6 f6 eb6 c6 g6>")
  .slow(14)
  .euclid(3, 11)
  .sound("sawtooth")
  .gain(sine.range(0.04, 0.14).slow(43))
  .attack(0.001)
  .decay(0.28)
  .sustain(0.0)
  .release(0.38)
  .lpf(4000)
  .lpq(11)
  .detune(perlin.range(-14, 14).slow(13))
  .off(0.382, x => x.gain(0.03).transpose(5).sound("sine").lpf(2600).room(0.80).pan(perlin.range(0.6, 0.92).slow(9)))
  .delay(0.40)
  .delaytime(0.382)
  .delayfeedback(0.36)
  .room(0.70)
  .size(0.86)
  .pan(perlin.range(0.1, 0.9).slow(11))
  .orbit(9)

// Deep sub pulse — breathing slowly
$: note("<g1 g1 c2 c2 d2 d2 bb1 bb1>")
  .slow(88)
  .sound("sine")
  .gain(sine.range(0.12, 0.32).slow(79))
  .attack(4.5)
  .decay(3.0)
  .sustain(0.55)
  .release(6.0)
  .lpf(sine.range(36, 220).slow(97))
  .lpq(0.6)
  .shape(sine.range(0, 0.20).slow(67))
  .room(0.14)
  .size(0.46)
  .pan(0.5)
  .orbit(10)

// Melodic halo — fifths, phaser glimmer
$: note("<d4 a4 g4 d5 f4 c5 eb4 bb4>")
  .slow(60)
  .palindrome()
  .sound("triangle")
  .gain(sine.range(0.04, 0.17).slow(97))
  .attack(1.2)
  .decay(1.6)
  .sustain(0.08)
  .release(2.4)
  .fmi(sine.range(0.0, 0.8).slow(79))
  .fmh(sine.range(1.5, 5.5).slow(103))
  .lpf(sine.range(1600, 4200).slow(79))
  .lpq(2.4)
  .detune(perlin.range(-6, 6).slow(29))
  .phaser(sine.range(0.10, 1.0).slow(89))
  .phaserdepth(sine.range(0.1, 0.9).slow(113))
  .off(0.25, x => x.transpose(7).gain(0.03).sound("sine").room(0.94).size(0.99))
  .delay(0.26)
  .delaytime(0.75)
  .delayfeedback(0.32)
  .room(0.84)
  .size(0.97)
  .pan(perlin.range(0.28, 0.72).slow(59))
  .orbit(11)

// Penumbra — slow descending dorian thirds
$: note("<bb3 g3 f3 eb3 d3 f3 g3 a3>")
  .slow(176)
  .palindrome()
  .sound("triangle")
  .gain(sine.range(0.05, 0.20).slow(107))
  .attack(4.2)
  .decay(3.2)
  .sustain(0.38)
  .release(7.0)
  .fmi(sine.range(0.05, 1.4).slow(89))
  .fmh(sine.range(2.0, 4.0).slow(127))
  .lpf(sine.range(520, 2200).slow(73))
  .lpq(1.6)
  .penv(sine.range(0.4, 2.8).slow(149))
  .pattack(1.4).pdecay(3.0)
  .trem(sine.range(0.02, 0.16).slow(121))
  .tremolosync(sine.range(18, 36).slow(83))
  .tremoloshape("sine")
  .room(0.86)
  .size(0.97)
  .pan(perlin.range(0.32, 0.68).slow(97))
  .orbit(18)

// Nocturne — sparse lyric line, high sine
$: note("<~ d6 ~ ~ ~ c6 ~ bb5 ~ ~ ~ a5 ~ g5 ~ ~ ~ f5 ~ eb5 ~>")
  .slow(128)
  .sound("sine")
  .gain(sine.range(0.06, 0.22).slow(139))
  .attack(1.8)
  .decay(3.0)
  .sustain(0.12)
  .release(5.5)
  .lpf(sine.range(2200, 6000).slow(71))
  .fmi(sine.range(0.0, 0.40).slow(97))
  .fmh(sine.range(3.5, 7.0).slow(113))
  .penv(2.0).pattack(0.30).pdecay(1.4)
  .delay(0.34)
  .delaytime(1.236)
  .delayfeedback(0.24)
  .room(0.92)
  .size(0.99)
  .pan(perlin.range(0.28, 0.72).slow(67))
  .orbit(19)

// Submerged bells — ring mod shimmer
$: note("<g3 ~ d4 ~ ~ f3 ~ c4 ~ bb3 ~ ~ eb4 ~ a3 ~>")
  .slow(108)
  .euclid(5, 16)
  .sound("triangle")
  .gain(sine.range(0.04, 0.15).slow(83))
  .attack(0.03)
  .decay(4.0)
  .sustain(0.0)
  .release(6.5)
  .detune(perlin.range(-10, 10).slow(43))
  .ring(sine.range(0.0, 0.28).slow(113))
  .ringf(sine.range(110, 520).slow(89))
  .lpf(sine.range(800, 3200).slow(97))
  .lpq(4.5)
  .delay(0.46)
  .delaytime(1.414)
  .delayfeedback(0.30)
  .room(0.90)
  .size(0.98)
  .pan(perlin.range(0.18, 0.82).slow(61))
  .orbit(20)

// Undertow — very low sine drone
$: note("<d2 ~ g2 ~ ~ c2 ~ bb1 ~ d2 ~ ~ f2 ~ c2 ~>")
  .slow(160)
  .sound("sine")
  .gain(sine.range(0.08, 0.28).slow(181))
  .attack(8.0)
  .decay(5.0)
  .sustain(0.65)
  .release(11.0)
  .lpf(sine.range(30, 160).slow(121))
  .lpq(0.4)
  .shape(sine.range(0.0, 0.12).slow(97))
  .room(0.10)
  .size(0.36)
  .pan(0.5)
  .orbit(21)

// Aureole — major-seventh tones floating high
$: note("<~ ~ fs5 ~ ~ a5 ~ ~ ~ cs6 ~ ~ ~ e6 ~ ~>")
  .slow(144)
  .sound("sine")
  .gain(sine.range(0.02, 0.11).slow(163))
  .attack(3.6)
  .decay(5.0)
  .sustain(0.05)
  .release(9.0)
  .fmi(sine.range(0.0, 0.4).slow(109))
  .fmh(sine.range(5.0, 10.0).slow(139))
  .ring(sine.range(0.0, 0.08).slow(151))
  .ringf(sine.range(200, 680).slow(119))
  .lpf(sine.range(3000, 8000).slow(89))
  .lpq(0.8)
  .delay(0.38)
  .delaytime(1.618)
  .delayfeedback(0.18)
  .room(0.95)
  .size(0.99)
  .pan(perlin.range(0.24, 0.76).slow(89))
  .orbit(22)

// Amber — supersaw choir, now with iterating offset for slow rotation
$: note("<g3 bb3 eb4 g3 f3 bb3 d4 f3>")
  .slow(96)
  .iter(3)
  .sound("supersaw")
  .gain(sine.range(0.02, 0.12).slow(119))
  .attack(3.5)
  .decay(2.5)
  .sustain(0.26)
  .release(5.5)
  .detune(perlin.range(8, 22).slow(47))
  .bpf(sine.range(800, 2400).slow(89))
  .bpq(sine.range(1.4, 5.0).slow(67))
  .lpf(sine.range(1000, 3200).slow(107))
  .penv(sine.range(0.0, 1.2).slow(137))
  .pattack(2.0).pdecay(4.5)
  .room(0.78)
  .size(0.96)
  .pan(perlin.range(0.3, 0.7).slow(71))
  .orbit(23)

// Veil — high triangle filigree, euclidean
$: note("<eb6 d6 c6 bb5 a5 g5 f5 eb5>")
  .slow(72)
  .euclid(4, 11)
  .sound("triangle")
  .gain(sine.range(0.02, 0.09).slow(131))
  .attack(0.9)
  .decay(3.0)
  .sustain(0.05)
  .release(5.0)
  .detune(perlin.range(-22, 22).slow(23))
  .lpf(sine.range(3200, 8500).slow(61))
  .lpq(2.8)
  .phaser(sine.range(0.05, 0.5).slow(73))
  .phaserdepth(sine.range(0.3, 0.9).slow(103))
  .delay(0.42)
  .delaytime(0.809)
  .delayfeedback(0.36)
  .room(0.92)
  .size(0.99)
  .pan(perlin.range(0.15, 0.85).slow(47))
  .orbit(24)

// Lithograph — sawtooth vocal-choral, ascending modal arc
$: note("<d4 f4 g4 a4 c5 bb4 a4 g4 f4 eb4 d4 c4>")
  .slow(160)
  .iter(4)
  .sound("sawtooth")
  .gain(sine.range(0.02, 0.10).slow(127))
  .attack(5.0)
  .decay(3.5)
  .sustain(0.24)
  .release(7.5)
  .vowel(sine.range(0, 1).slow(79).segment(5))
  .lpf(sine.range(700, 2600).slow(101))
  .lpq(2.0)
  .hpf(sine.range(90, 260).slow(83))
  .detune(perlin.range(-5, 5).slow(61))
  .room(0.82)
  .size(0.97)
  .pan(perlin.range(0.35, 0.65).slow(89))
  .orbit(25)

// Sediment — very slow sine cluster, microtonal drift
$: note("<g2 a2 g2 bb2 a2 g2 f2 g2>")
  .slow(256)
  .iter(7)
  .sound("sine")
  .gain(sine.range(0.0, 0.14).slow(211))
  .attack(12.0)
  .decay(8.0)
  .sustain(0.50)
  .release(16.0)
  .detune(perlin.range(-18, 18).slow(37))
  .lpf(sine.range(60, 340).slow(137))
  .lpq(0.5)
  .shape(sine.range(0.0, 0.07).slow(103))
  .off(0.05, x => x.detune(perlin.range(8, 24).slow(53)).gain(0.06).lpf(200))
  .off(0.11, x => x.detune(perlin.range(-24, -8).slow(67)).gain(0.05).lpf(180))
  .room(0.08)
  .size(0.28)
  .pan(sine.range(0.44, 0.56).slow(173))
  .orbit(29)

// Membrane — slow pulse drone, mid-low register, tremolo breathing
$: note("<g3 ~ ~ f3 ~ ~ eb3 ~ ~ d3 ~ ~ f3 ~ ~ g3>")
  .slow(192)
  .sound("pulse")
  .gain(sine.range(0.03, 0.16).slow(167))
  .attack(5.5)
  .decay(4.0)
  .sustain(0.32)
  .release(8.0)
  .lpf(sine.range(320, 1100).slow(89))
  .lpq(1.8)
  .hpf(sine.range(55, 130).slow(137))
  .fmi(sine.range(0.0, 1.2).slow(103))
  .fmh(sine.range(1.0, 3.5).slow(149))
  .trem(sine.range(0.04, 0.22).slow(97))
  .tremolosync(sine.range(12, 28).slow(71))
  .tremoloshape("sine")
  .detune(perlin.range(-9, 9).slow(53))
  .off(0.16, x => x.transpose(5).gain(0.04).sound("sine").lpf(900).room(0.88))
  .room(0.68)
  .size(0.94)
  .pan(perlin.range(0.28, 0.72).slow(113))
  .orbit(33)

// Cartography — sparse triangle chords mapping the modal space
$: note("<g4 d5 ~ ~ bb4 f5 ~ ~ c5 g5 ~ ~ a4 e5 ~ ~>")
  .slow(384)
  .sound("triangle")
  .gain(sine.range(0.01, 0.08).slow(251))
  .attack(9.0)
  .decay(7.0)
  .sustain(0.18)
  .release(15.0)
  .fmi(sine.range(0.0, 0.7).slow(191))
  .fmh(sine.range(3.0, 7.0).slow(223))
  .lpf(sine.range(1200, 5000).slow(127))
  .lpq(0.9)
  .detune(perlin.range(-12, 12).slow(79))
  .phaser(sine.range(0.02, 0.3).slow(163))
  .phaserdepth(sine.range(0.1, 0.6).slow(197))
  .delay(0.35)
  .delaytime(2.0)
  .delayfeedback(0.15)
  .room(0.96)
  .size(0.99)
  .pan(perlin.range(0.20, 0.80).slow(179))
  .orbit(34)

// Filament — high supersaw, very slow crossfade arc
$: note("<~ ~ ~ eb5 ~ ~ ~ g5 ~ ~ ~ bb5 ~ ~ ~ ~ ~ ~ ~ d6 ~ ~ ~ c6 ~ ~ ~ bb5 ~ ~ ~>")
  .slow(448)
  .sound("supersaw")
  .gain(sine.range(0.0, 0.07).slow(307))
  .attack(11.0)
  .decay(8.0)
  .sustain(0.12)
  .release(18.0)
  .detune(perlin.range(14, 34).slow(89))
  .lpf(sine.range(1800, 5500).slow(151))
  .lpq(0.7)
  .hpf(sine.range(300, 800).slow(113))
  .fmi(sine.range(0.0, 0.5).slow(173))
  .fmh(sine.range(4.0, 9.0).slow(211))
  .penv(sine.range(0.2, 1.8).slow(197))
  .pattack(3.0).pdecay(6.0)
  .phaser(sine.range(0.04, 0.4).slow(131))
  .phaserdepth(sine.range(0.1, 0.5).slow(167))
  .delay(0.32)
  .delaytime(1.272)
  .delayfeedback(0.19)
  .room(0.94)
  .size(0.99)
  .pan(perlin.range(0.22, 0.78).slow(199))
  .orbit(35)

// Ossuary — low square drone, bitcrushed warmth
$: note("<g2 ~ ~ ~ f2 ~ ~ ~ eb2 ~ ~ ~ d2 ~ ~ ~ g2 ~ ~ ~ bb2 ~ ~ ~ c3 ~ ~ ~ d3 ~ ~ ~>")
  .slow(512)
  .sound("square")
  .gain(sine.range(0.0, 0.11).slow(277))
  .attack(14.0)
  .decay(10.0)
  .sustain(0.44)
  .release(20.0)
  .lpf(sine.range(80, 480).slow(163))
  .lpq(1.1)
  .hpf(30)
  .crush(sine.range(6, 14).slow(241))
  .shape(sine.range(0.0, 0.10).slow(139))
  .detune(perlin.range(-6, 6).slow(97))
  .trem(sine.range(0.02, 0.12).slow(193))
  .tremolosync(sine.range(24, 48).slow(127))
  .tremoloshape("sine")
  .room(0.18)
  .size(0.52)
  .pan(sine.range(0.46, 0.54).slow(229))
  .orbit(36)

// Liminal — sparse single high sine tones, like distant bells dissolving
$: note("<~ ~ ~ ~ ~ ~ ~ g6 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ f6 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ eb6 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ d6>")
  .slow(768)
  .sound("sine")
  .gain(sine.range(0.03, 0.14).slow(347))
  .attack(6.0)
  .decay(12.0)
  .sustain(0.0)
  .release(20.0)
  .fmi(sine.range(0.0, 0.3).slow(197))
  .fmh(sine.range(6.0, 12.0).slow(251))
  .lpf(sine.range(4000, 10000).slow(113))
  .ring(sine.range(0.0, 0.06).slow(179))
  .ringf(sine.range(300, 900).slow(157))
  .detune(perlin.range(-5, 5).slow(83))
  .delay(0.44)
  .delaytime(2.618)
  .delayfeedback(0.12)
  .room(0.98)
  .size(0.99)
  .pan(perlin.range(0.18, 0.82).slow(263))
  .orbit(37)

// Palimpsest — mid sawtooth, slow modal arc, new voice
$: note("<g3 a3 c4 d4 f4 eb4 d4 c4 bb3 a3 g3 f3>")
  .slow(224)
  .iter(5)
  .sound("sawtooth")
  .gain(sine.range(0.01, 0.08).slow(173))
  .attack(7.0)
  .decay(5.0)
  .sustain(0.20)
  .release(10.0)
  .vowel(sine.range(0, 1).slow(113).segment(4))
  .lpf(sine.range(500, 2000).slow(97))
  .lpq(1.8)
  .hpf(sine.range(60, 180).slow(127))
  .detune(perlin.range(-8, 8).slow(71))
  .fmi(sine.range(0.0, 0.6).slow(149))
  .fmh(sine.range(2.0, 5.0).slow(191))
  .trem(sine.range(0.02, 0.14).slow(139))
  .tremolosync(sine.range(20, 40).slow(103))
  .tremoloshape("sine")
  .room(0.74)
  .size(0.95)
  .pan(perlin.range(0.25, 0.75).slow(107))
  .orbit(38)

// Crepuscule — descending sine thirds, very slow, new harmonic color
$: note("<~ bb4 ~ ~ ~ g4 ~ ~ ~ eb4 ~ ~ ~ c4 ~ ~ ~ d4 ~ ~ ~ f4 ~ ~ ~ a4 ~ ~ ~ g4 ~ ~>")
  .slow(336)
  .sound("sine")
  .gain(sine.range(0.02, 0.12).slow(239))
  .attack(10.0)
  .decay(8.0)
  .sustain(0.22)
  .release(14.0)
  .fmi(sine.range(0.0, 0.5).slow(167))
  .fmh(sine.range(3.0, 8.0).slow(211))
  .lpf(sine.range(800, 3800).slow(131))
  .lpq(1.0)
  .ring(sine.range(0.0, 0.10).slow(193))
  .ringf(sine.range(150, 600).slow(149))
  .detune(perlin.range(-7, 7).slow(89))
  .phaser(sine.range(0.03, 0.35).slow(157))
  .phaserdepth(sine.range(0.1, 0.7).slow(199))
  .delay(0.40)
  .delaytime(1.732)
  .delayfeedback(0.16)
  .room(0.93)
  .size(0.99)
  .pan(perlin.range(0.20, 0.80).slow(181))
  .orbit(39)