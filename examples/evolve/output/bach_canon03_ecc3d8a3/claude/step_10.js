setcps(100/60/4)

let upperVoice = x => x
  .sound("triangle")
  .gain(0.42)
  .attack(0.03)
  .decay(0.28)
  .sustain(0.14)
  .release(0.22)
  .lpf(sine.range(1600, 3400).slow(37))
  .room(0.38)
  .size(0.7)

let lowerVoice = x => x
  .sound("square")
  .gain(0.24)
  .attack(0.03)
  .decay(0.22)
  .sustain(0.14)
  .release(0.16)
  .lpf(sine.range(700, 1200).slow(41))
  .lpq(2)
  .room(0.32)
  .size(0.65)

let bassVoice = x => x
  .sound("sine")
  .gain(0.48)
  .attack(0.08)
  .decay(0.5)
  .sustain(0.32)
  .release(0.4)
  .lpf(380)
  .room(0.22)
  .size(0.58)

let innerVoice = x => x
  .sound("triangle")
  .gain(0.22)
  .attack(0.06)
  .decay(0.35)
  .sustain(0.18)
  .release(0.25)
  .lpf(sine.range(1200, 2400).slow(29))
  .room(0.38)
  .size(0.7)

// Upper voice — subject with canon echo, pitch bloom
$: note("~!32 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 eb5 ~!11 d5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 eb6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!23")
  .slow(240/8/4)
  .apply(upperVoice)
  .penv(3).pattack(0.04).pdecay(0.18)
  .off(0.125, x => x.transpose(7).gain(0.13).lpf(2000))
  .off(0.5, x => x.transpose(-5).gain(0.08).sound("sine").lpf(1400).room(0.6))
  .sometimes(x => x.detune(perlin.range(-12, 12)))
  .pan(sine.range(0.18, 0.44).slow(31))
  .orbit(1)

// Middle voice — answer at fifth below, leslie warmth
$: note("~!12 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!11 c5 ~!11 bb4 ~!11 a4 ~!11 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!23")
  .slow(240/8/4)
  .apply(lowerVoice)
  .leslie(sine.range(0.1, 0.7).slow(19))
  .lrate(sine.range(0.5, 1.2).slow(43))
  .rarely(x => x.vowel("<a e i o>").gain(0.32))
  .pan(sine.range(0.56, 0.82).slow(23))
  .orbit(2)

// Inner voice — harmonic filler, tremolo, with echo shadow
$: note("~!20 bb4 ~!23 a4 ~!23 g4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 f4 ~!19")
  .slow(240/8/4)
  .apply(innerVoice)
  .trem(sine.range(0.1, 0.35).slow(67))
  .tremolosync(sine.range(8, 16).slow(37))
  .tremoloshape("sine")
  .off(0.333, x => x.gain(0.07).transpose(12).sound("sine").lpf(1800).room(0.7))
  .pan(sine.range(0.38, 0.62).slow(19))
  .orbit(3)

// Bass — walking harmonic motion, dorian, now with subtle octave echo
$: note("<g2 g2 c3 c3 d3 d3 eb3 eb3 d3 d3 bb2 bb2 c3 c3 d3 d3>")
  .slow(20)
  .apply(bassVoice)
  .ring(sine.range(0, 0.14).slow(53))
  .ringf(sine.range(60, 180).slow(37))
  .off(0.25, x => x.transpose(12).gain(0.09).sound("triangle").lpf(500).room(0.4))
  .off(0.5, x => x.transpose(-12).gain(0.18).sound("sine").lpf(120).room(0.15))
  .pan(sine.range(0.44, 0.56).slow(61))
  .orbit(4)

// Shimmer — high register, golden ratio delay, phaser
$: note("~!32 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 bb6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!23")
  .slow(240/8/4)
  .sound("sine")
  .gain(sine.range(0.05, 0.11).slow(71))
  .attack(0.14)
  .decay(0.7)
  .sustain(0.05)
  .release(0.65)
  .phaser(sine.range(0.3, 1.8).slow(37))
  .phaserdepth(sine.range(0.2, 1.1).slow(53))
  .phasersweep(sine.range(800, 4000).slow(89))
  .lpf(5000)
  .delay(0.55)
  .delaytime(0.618)
  .delayfeedback(0.44)
  .room(0.75)
  .size(0.92)
  .pan(sine.range(0.25, 0.75).slow(43))
  .orbit(5)

// Breath pad — slow FM, evolving, breathing lpf/hpf
$: note("<g3 bb3 d4 f4 g3 eb4 d4 c4>")
  .slow(32)
  .sound("triangle")
  .gain(sine.range(0.04, 0.16).slow(47))
  .attack(1.4)
  .decay(1.0)
  .sustain(0.45)
  .release(1.8)
  .fmi(sine.range(0.1, 1.8).slow(71))
  .fmh(sine.range(1.5, 6.0).slow(113))
  .lpf(sine.range(260, 1400).slow(53))
  .hpf(sine.range(40, 220).slow(89))
  .lpq(1.6)
  .room(0.65)
  .size(0.9)
  .pan(sine.range(0.35, 0.65).slow(79))
  .orbit(6)

// Counter-melody — supersaw, bpf resonance, perlin detune drift
$: note("~!48 eb4 ~!23 d4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 c4 ~!11")
  .slow(240/8/4)
  .iter(7)
  .sound("supersaw")
  .gain(0.09)
  .attack(0.12)
  .decay(0.5)
  .sustain(0.18)
  .release(0.4)
  .bpf(sine.range(500, 1600).slow(31))
  .bpq(sine.range(1.5, 6).slow(23))
  .detune(perlin.range(3, 22).slow(17))
  .juxBy(0.25, x => x.detune(perlin.range(-22, -3).slow(23)).gain(0.07))
  .room(0.5)
  .size(0.76)
  .pan(sine.range(0.3, 0.7).slow(17))
  .orbit(7)

// Slow cantus — descending dorian anchor, jux reverses for echo shadow
$: note("<d5 c5 bb4 a4 g4 f4 eb4 d4>")
  .slow(96)
  .sound("triangle")
  .gain(sine.range(0.08, 0.22).slow(83))
  .attack(2.2)
  .decay(1.5)
  .sustain(0.55)
  .release(2.8)
  .fmi(sine.range(0.05, 0.7).slow(97))
  .fmh(sine.range(2.0, 5.0).slow(131))
  .lpf(sine.range(600, 2200).slow(67))
  .lpq(1.2)
  .juxBy(0.4, x => x.rev().room(0.88).size(0.98).gain(0.06).transpose(7))
  .room(0.72)
  .size(0.94)
  .orbit(8)

// Sparse high pings — sawtooth, euclidean, perlin pan, pitch drift
$: note("~!64 <d6 g6 bb5 a6 f6 eb6 c6 g6>")
  .slow(240/8/4)
  .euclid(3, 8)
  .sound("sawtooth")
  .gain(sine.range(0.04, 0.1).slow(41))
  .attack(0.001)
  .decay(0.18)
  .sustain(0.0)
  .release(0.28)
  .lpf(4500)
  .lpq(9)
  .detune(perlin.range(-12, 12).slow(11))
  .delay(0.45)
  .delaytime(0.382)
  .delayfeedback(0.42)
  .room(0.65)
  .size(0.82)
  .pan(perlin.range(0.1, 0.9).slow(7))
  .orbit(9)

// Deep sub pulse — sine, very slow, harmonic root, gentle shape
$: note("<g1 g1 c2 c2 d2 d2 bb1 bb1>")
  .slow(64)
  .sound("sine")
  .gain(sine.range(0.1, 0.28).slow(59))
  .attack(3.0)
  .decay(2.0)
  .sustain(0.6)
  .release(4.0)
  .lpf(sine.range(45, 180).slow(43))
  .lpq(0.8)
  .shape(sine.range(0, 0.15).slow(71))
  .room(0.18)
  .size(0.5)
  .pan(0.5)
  .orbit(10)

// Melodic halo — superimposed fifths, phaser glimmer, now with fm shimmer
$: note("<d4 a4 g4 d5 f4 c5 eb4 bb4>")
  .slow(48)
  .sound("triangle")
  .gain(sine.range(0.03, 0.13).slow(89))
  .attack(0.8)
  .decay(1.2)
  .sustain(0.08)
  .release(1.6)
  .fmi(sine.range(0.0, 0.4).slow(59))
  .fmh(sine.range(1.0, 3.0).slow(83))
  .lpf(sine.range(1200, 3600).slow(61))
  .lpq(2.5)
  .phaser(sine.range(0.1, 0.9).slow(73))
  .phaserdepth(sine.range(0.1, 0.7).slow(97))
  .off(0.25, x => x.transpose(7).gain(0.04).sound("sine").room(0.9).size(0.97))
  .delay(0.3)
  .delaytime(0.75)
  .delayfeedback(0.32)
  .room(0.8)
  .size(0.95)
  .pan(perlin.range(0.3, 0.7).slow(53))
  .orbit(11)

// Spectral haze — very slow chord wash, pure sine, whisper-quiet
$: note("<g3 d4 f3 c4 bb3 f4 eb3 bb3>")
  .slow(128)
  .sound("sine")
  .gain(sine.range(0.02, 0.09).slow(103))
  .attack(4.0)
  .decay(3.0)
  .sustain(0.5)
  .release(6.0)
  .lpf(sine.range(300, 1100).slow(79))
  .lpq(0.8)
  .room(0.9)
  .size(0.99)
  .pan(perlin.range(0.2, 0.8).slow(67))
  .orbit(12)

// New: ghost canon — whispered fifth echo of upper voice, very soft pulse
$: note("<g4 a4 bb4 c5 d5 c5 bb4 a4>")
  .slow(64)
  .sound("sine")
  .gain(sine.range(0.02, 0.07).slow(109))
  .attack(1.6)
  .decay(2.0)
  .sustain(0.3)
  .release(3.0)
  .lpf(sine.range(400, 1000).slow(83))
  .lpq(0.6)
  .delay(0.35)
  .delaytime(1.0)
  .delayfeedback(0.36)
  .room(0.85)
  .size(0.97)
  .pan(perlin.range(0.25, 0.75).slow(91))
  .orbit(13)