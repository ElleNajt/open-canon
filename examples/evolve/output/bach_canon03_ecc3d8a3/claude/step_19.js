setcps(92/60/4)

let upperVoice = x => x
  .sound("triangle")
  .gain(0.40)
  .attack(0.04)
  .decay(0.32)
  .sustain(0.16)
  .release(0.28)
  .lpf(sine.range(1400, 3200).slow(41))
  .room(0.42)
  .size(0.72)

let lowerVoice = x => x
  .sound("square")
  .gain(0.22)
  .attack(0.04)
  .decay(0.24)
  .sustain(0.12)
  .release(0.18)
  .lpf(sine.range(600, 1100).slow(47))
  .lpq(2.2)
  .room(0.35)
  .size(0.68)

let bassVoice = x => x
  .sound("sine")
  .gain(0.45)
  .attack(0.10)
  .decay(0.55)
  .sustain(0.30)
  .release(0.45)
  .lpf(420)
  .room(0.20)
  .size(0.55)

let innerVoice = x => x
  .sound("triangle")
  .gain(0.20)
  .attack(0.07)
  .decay(0.38)
  .sustain(0.16)
  .release(0.28)
  .lpf(sine.range(1100, 2200).slow(33))
  .room(0.40)
  .size(0.72)

// Upper voice — subject with canon echo
$: note("~!32 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 eb5 ~!11 d5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 c6 ~!11 d6 ~!11 eb6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 g5 ~!23")
  .slow(240/8/4)
  .apply(upperVoice)
  .penv(2.5).pattack(0.05).pdecay(0.22)
  .off(0.125, x => x.transpose(7).gain(0.11).lpf(1800))
  .off(0.5, x => x.transpose(-5).gain(0.07).sound("sine").lpf(1200).room(0.65))
  .sometimes(x => x.detune(perlin.range(-8, 8)))
  .pan(sine.range(0.16, 0.42).slow(37))
  .orbit(1)

// Middle voice — answer at fifth below
$: note("~!12 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!11 c5 ~!11 bb4 ~!11 a4 ~!11 d5 ~!11 e5 ~!11 f5 ~!11 g5 ~!11 a5 ~!11 bb5 ~!11 a5 ~!11 g5 ~!11 f5 ~!11 e5 ~!11 d5 ~!23")
  .slow(240/8/4)
  .apply(lowerVoice)
  .leslie(sine.range(0.08, 0.6).slow(23))
  .lrate(sine.range(0.4, 1.0).slow(47))
  .rarely(x => x.vowel("<a e i o>").gain(0.28))
  .pan(sine.range(0.58, 0.84).slow(29))
  .orbit(2)

// Inner voice — harmonic filler
$: note("~!20 bb4 ~!23 a4 ~!23 g4 ~!23 f4 ~!23 g4 ~!23 eb4 ~!23 d4 ~!23 f4 ~!19")
  .slow(240/8/4)
  .apply(innerVoice)
  .trem(sine.range(0.08, 0.38).slow(71))
  .tremolosync(sine.range(8, 20).slow(43))
  .tremoloshape("sine")
  .off(0.333, x => x.gain(0.06).transpose(12).sound("sine").lpf(1600).room(0.72))
  .pan(sine.range(0.36, 0.64).slow(23))
  .orbit(3)

// Bass — walking harmonic motion
$: note("<g2 g2 c3 c3 d3 d3 eb3 eb3 d3 d3 bb2 bb2 c3 c3 d3 d3>")
  .slow(22)
  .apply(bassVoice)
  .off(0.25, x => x.transpose(12).gain(0.08).sound("triangle").lpf(480).room(0.38))
  .off(0.5, x => x.transpose(-12).gain(0.20).sound("sine").lpf(110).room(0.14))
  .shape(sine.range(0, 0.18).slow(83))
  .pan(sine.range(0.42, 0.58).slow(67))
  .orbit(4)

// Shimmer — high sine, golden ratio delay
$: note("~!32 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!11 c6 ~!11 bb5 ~!11 a5 ~!11 d6 ~!11 e6 ~!11 f6 ~!11 g6 ~!11 a6 ~!11 bb6 ~!11 a6 ~!11 g6 ~!11 f6 ~!11 e6 ~!11 d6 ~!23")
  .slow(240/8/4)
  .sound("sine")
  .gain(sine.range(0.04, 0.20).slow(59))
  .attack(0.16)
  .decay(0.8)
  .sustain(0.05)
  .release(0.7)
  .fmi(sine.range(0.0, 0.9).slow(67))
  .fmh(sine.range(2.5, 6.0).slow(103))
  .phaser(sine.range(0.2, 2.0).slow(41))
  .phaserdepth(sine.range(0.2, 1.4).slow(61))
  .phasersweep(sine.range(800, 4800).slow(97))
  .lpf(5200)
  .delay(0.52)
  .delaytime(0.618)
  .delayfeedback(0.40)
  .room(0.78)
  .size(0.93)
  .pan(sine.range(0.22, 0.78).slow(47))
  .orbit(5)

// Breath pad — slow FM, pulse
$: note("<g3 bb3 d4 f4 g3 eb4 d4 c4>")
  .slow(36)
  .sound("pulse")
  .gain(sine.range(0.03, 0.17).slow(53))
  .attack(2.2)
  .decay(1.5)
  .sustain(0.35)
  .release(2.8)
  .fmi(sine.range(0.1, 2.4).slow(79))
  .fmh(sine.range(1.5, 6.0).slow(127))
  .lpf(sine.range(200, 1600).slow(59))
  .hpf(sine.range(45, 180).slow(97))
  .lpq(1.6)
  .ring(sine.range(0.0, 0.18).slow(89))
  .ringf(sine.range(70, 340).slow(71))
  .room(0.70)
  .size(0.92)
  .pan(sine.range(0.33, 0.67).slow(83))
  .orbit(6)

// Slow cantus — descending dorian anchor
$: note("<d5 c5 bb4 a4 g4 f4 eb4 d4>")
  .slow(108)
  .sound("triangle")
  .gain(sine.range(0.12, 0.32).slow(89))
  .attack(2.5)
  .decay(1.8)
  .sustain(0.52)
  .release(3.2)
  .fmi(sine.range(0.05, 1.1).slow(103))
  .fmh(sine.range(2.0, 7.0).slow(137))
  .lpf(sine.range(800, 4200).slow(71))
  .lpq(1.4)
  .triode(sine.range(0.0, 0.20).slow(113))
  .juxBy(0.42, x => x.rev().room(0.92).size(0.98).gain(0.06).transpose(7).lpf(1300))
  .room(0.74)
  .size(0.95)
  .orbit(8)

// Sparse high pings — sawtooth, euclidean
$: note("<d6 g6 bb5 a6 f6 eb6 c6 g6>")
  .slow(12)
  .euclid(3, 8)
  .sound("sawtooth")
  .gain(sine.range(0.05, 0.16).slow(43))
  .attack(0.001)
  .decay(0.22)
  .sustain(0.0)
  .release(0.32)
  .lpf(4200)
  .lpq(13)
  .detune(perlin.range(-14, 14).slow(13))
  .off(0.382, x => x.gain(0.04).transpose(5).sound("sine").lpf(2800).room(0.78).pan(perlin.range(0.6, 0.92).slow(9)))
  .delay(0.42)
  .delaytime(0.382)
  .delayfeedback(0.38)
  .room(0.68)
  .size(0.84)
  .pan(perlin.range(0.1, 0.9).slow(9))
  .orbit(9)

// Deep sub pulse
$: note("<g1 g1 c2 c2 d2 d2 bb1 bb1>")
  .slow(72)
  .sound("sine")
  .gain(sine.range(0.14, 0.36).slow(67))
  .attack(3.5)
  .decay(2.2)
  .sustain(0.58)
  .release(4.5)
  .lpf(sine.range(38, 260).slow(79))
  .lpq(0.7)
  .shape(sine.range(0, 0.24).slow(59))
  .room(0.16)
  .size(0.48)
  .pan(0.5)
  .orbit(10)

// Melodic halo — fifths, phaser glimmer
$: note("<d4 a4 g4 d5 f4 c5 eb4 bb4>")
  .slow(56)
  .palindrome()
  .sound("triangle")
  .gain(sine.range(0.04, 0.19).slow(97))
  .attack(1.0)
  .decay(1.4)
  .sustain(0.10)
  .release(2.0)
  .fmi(sine.range(0.0, 1.0).slow(67))
  .fmh(sine.range(1.5, 6.0).slow(89))
  .lpf(sine.range(1400, 4400).slow(67))
  .lpq(2.8)
  .phaser(sine.range(0.12, 1.2).slow(79))
  .phaserdepth(sine.range(0.1, 1.0).slow(103))
  .off(0.25, x => x.transpose(7).gain(0.04).sound("sine").room(0.92).size(0.98))
  .delay(0.28)
  .delaytime(0.75)
  .delayfeedback(0.34)
  .room(0.82)
  .size(0.96)
  .pan(perlin.range(0.28, 0.72).slow(59))
  .orbit(11)

// Penumbra — slow descending dorian thirds, now with pitch envelope bloom
$: note("<bb3 g3 f3 eb3 d3 f3 g3 a3>")
  .slow(160)
  .palindrome()
  .sound("triangle")
  .gain(sine.range(0.05, 0.22).slow(97))
  .attack(3.8)
  .decay(2.8)
  .sustain(0.42)
  .release(6.0)
  .fmi(sine.range(0.05, 1.6).slow(79))
  .fmh(sine.range(2.0, 4.5).slow(113))
  .lpf(sine.range(480, 2400).slow(67))
  .lpq(1.8)
  .penv(sine.range(0.5, 3.0).slow(131))
  .pattack(1.2).pdecay(2.5)
  .trem(sine.range(0.02, 0.18).slow(109))
  .tremolosync(sine.range(16, 32).slow(73))
  .tremoloshape("sine")
  .room(0.84)
  .size(0.97)
  .pan(perlin.range(0.32, 0.68).slow(83))
  .orbit(18)

// Nocturne — sparse lyric line, high sine
$: note("<~ d6 ~ ~ c6 ~ bb5 ~ ~ a5 ~ g5 ~ ~ f5 ~ eb5 ~>")
  .slow(108)
  .sound("sine")
  .gain(sine.range(0.07, 0.24).slow(127))
  .attack(1.6)
  .decay(2.6)
  .sustain(0.16)
  .release(4.5)
  .lpf(sine.range(2000, 5800).slow(63))
  .fmi(sine.range(0.0, 0.45).slow(83))
  .fmh(sine.range(3.5, 6.5).slow(101))
  .penv(1.8).pattack(0.25).pdecay(1.1)
  .delay(0.36)
  .delaytime(1.236)
  .delayfeedback(0.26)
  .room(0.90)
  .size(0.98)
  .pan(perlin.range(0.28, 0.72).slow(67))
  .orbit(19)

// Submerged bells — ring mod shimmer, now drifting in pitch
$: note("<g3 ~ d4 ~ ~ f3 ~ c4 ~ bb3 ~ ~ eb4 ~ a3 ~>")
  .slow(96)
  .euclid(5, 16)
  .sound("triangle")
  .gain(sine.range(0.05, 0.17).slow(73))
  .attack(0.02)
  .decay(3.2)
  .sustain(0.0)
  .release(5.0)
  .detune(perlin.range(-12, 12).slow(37))
  .ring(sine.range(0.0, 0.34).slow(103))
  .ringf(sine.range(90, 560).slow(79))
  .lpf(sine.range(700, 3400).slow(89))
  .lpq(5.0)
  .delay(0.48)
  .delaytime(1.414)
  .delayfeedback(0.32)
  .room(0.88)
  .size(0.97)
  .pan(perlin.range(0.18, 0.82).slow(53))
  .orbit(20)

// Undertow — very low sine drone, now with slow shape swell
$: note("<d2 ~ g2 ~ ~ c2 ~ bb1 ~ d2 ~ ~ f2 ~ c2 ~>")
  .slow(144)
  .sound("sine")
  .gain(sine.range(0.09, 0.30).slow(167))
  .attack(7.0)
  .decay(4.5)
  .sustain(0.68)
  .release(9.0)
  .lpf(sine.range(28, 180).slow(107))
  .lpq(0.5)
  .shape(sine.range(0.0, 0.14).slow(89))
  .room(0.10)
  .size(0.38)
  .pan(0.5)
  .orbit(21)

// Aureole — major-seventh tones floating high, now with faint ring shimmer
$: note("<~ ~ fs5 ~ ~ a5 ~ ~ ~ cs6 ~ ~ ~ e6 ~ ~>")
  .slow(128)
  .sound("sine")
  .gain(sine.range(0.02, 0.13).slow(149))
  .attack(3.2)
  .decay(4.0)
  .sustain(0.06)
  .release(7.5)
  .fmi(sine.range(0.0, 0.5).slow(97))
  .fmh(sine.range(4.5, 9.0).slow(127))
  .ring(sine.range(0.0, 0.10).slow(137))
  .ringf(sine.range(180, 720).slow(109))
  .lpf(sine.range(2800, 7500).slow(79))
  .lpq(1.0)
  .delay(0.40)
  .delaytime(1.618)
  .delayfeedback(0.20)
  .room(0.94)
  .size(0.99)
  .pan(perlin.range(0.24, 0.76).slow(83))
  .orbit(22)

// Amber — supersaw choir, dorian iv warmth
$: note("<g3 bb3 eb4 g3 f3 bb3 d4 f3>")
  .slow(88)
  .palindrome()
  .sound("supersaw")
  .gain(sine.range(0.03, 0.14).slow(107))
  .attack(3.0)
  .decay(2.0)
  .sustain(0.32)
  .release(4.5)
  .detune(perlin.range(6, 24).slow(41))
  .bpf(sine.range(700, 2600).slow(79))
  .bpq(sine.range(1.2, 5.5).slow(59))
  .lpf(sine.range(900, 3000).slow(97))
  .room(0.76)
  .size(0.95)
  .pan(perlin.range(0.3, 0.7).slow(63))
  .orbit(23)

// Veil — high triangle filigree, euclidean, perlin pitch wander
$: note("<eb6 d6 c6 bb5 a5 g5 f5 eb5>")
  .slow(64)
  .euclid(4, 11)
  .sound("triangle")
  .gain(sine.range(0.02, 0.10).slow(119))
  .attack(0.8)
  .decay(2.5)
  .sustain(0.06)
  .release(4.0)
  .detune(perlin.range(-20, 20).slow(19))
  .lpf(sine.range(3000, 8000).slow(53))
  .lpq(3.0)
  .phaser(sine.range(0.05, 0.6).slow(61))
  .phaserdepth(sine.range(0.3, 1.0).slow(89))
  .delay(0.44)
  .delaytime(0.809)
  .delayfeedback(0.38)
  .room(0.90)
  .size(0.98)
  .pan(perlin.range(0.15, 0.85).slow(43))
  .orbit(24)

// Lithograph — new: slow sawtooth voice in middle register, iterating through dorian modes
// Thick formant sweep gives it a vocal-choral quality distinct from all existing layers
$: note("<d4 f4 g4 a4 c5 bb4 a4 g4 f4 eb4 d4 c4>")
  .slow(144)
  .iter(3)
  .sound("sawtooth")
  .gain(sine.range(0.03, 0.12).slow(113))
  .attack(4.5)
  .decay(3.0)
  .sustain(0.28)
  .release(6.5)
  .vowel(sine.range(0, 1).slow(67).segment(5))
  .lpf(sine.range(600, 2800).slow(89))
  .lpq(2.4)
  .hpf(sine.range(80, 280).slow(71))
  .detune(perlin.range(-6, 6).slow(53))
  .room(0.80)
  .size(0.96)
  .pan(perlin.range(0.35, 0.65).slow(79))
  .orbit(25)

// Filament — new: very sparse high pulse, euclidean 2/13, long FM decay
// Traces a counter-melody to nocturne, barely audible but texturally present
$: note("<~ a6 ~ ~ ~ f6 ~ ~ eb6 ~ ~ ~ g6 ~ ~ ~>")
  .slow(192)
  .euclid(2, 13)
  .sound("sine")
  .gain(sine.range(0.01, 0.07).slow(163))
  .attack(2.8)
  .decay(5.5)
  .sustain(0.04)
  .release(8.0)
  .fmi(sine.range(0.0, 1.2).slow(107))
  .fmh(sine.range(6.0, 12.0).slow(149))
  .lpf(sine.range(4000, 9000).slow(61))
  .phaser(sine.range(0.03, 0.4).slow(97))
  .phaserdepth(sine.range(0.1, 0.8).slow(127))
  .delay(0.55)
  .delaytime(2.0)
  .delayfeedback(0.18)
  .room(0.96)
  .size(0.99)
  .pan(perlin.range(0.2, 0.8).slow(101))
  .orbit(26)