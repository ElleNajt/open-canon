setcps(93/60/4)

$: stack(
  note("g3 gb3 e3 <d3 b2> c3 d3 g2").slow(3)
    .sound("triangle")
    .gain(0.38)
    .lpf(sine.range(990, 3400).slow(11))
    .lpq(6.9)
    .every(5, x => x.rev())
    .every(4, x => x.pan(sine.range(0.18,0.62).slow(9))),
  s("bd*3 <bd bd~>")
    .gain(0.19)
    .room(0.27)
    .cutoff(890)
    .every(6, x => x.echo(0.17,0.13,0.15))
)

$: s("hh cp <cp [cp~]> hh").slow(3)
  .gain(0.15)
  .pan(perlin.range(0.09,0.71).slow(7))
  .cutoff(3760)
  .hpf(sine.range(490, 710).slow(10))
  .every(4, x => x.rev())

$: stack(
  note("<c4 e4 g4 eb4 d4 f4 eb4>").slow(8)
    .sound("supersaw")
    .gain(0.18)
    .pan(sine.range(0.18,0.88).slow(7))
    .cutoff(3560)
    .echo(0.18,0.17,0.14),
  note("a3 <d4~ f4> g3").slow(10)
    .sound("supersaw")
    .gain(0.11)
    .lpf(1320)
    .tremolo(0.39)
)

$: note("g4 d4 [f4~] <e4 a4>").slow(5)
  .sound("piano")
  .gain(0.14)
  .release(1)
  .room(0.44)
  .every(6, x => x.pan(rand.range(0.21,0.81)))
  .every(3, x => x.rev())
  .every(8, x => x.cutoff(sine.range(1400,5200).slow(8)))

$: s("sine supersaw triangle").slow(13)
  .gain(0.09)
  .pan(perlin.range(0.24,0.91).slow(7))
  .tremolo(sine.range(0.23,0.37).slow(12))
  .cutoff(sine.range(3100, 7400).slow(9))
  .every(6, x => x.jux(y => y.rev().cutoff(4444)))

$: note("<eb3~ f3~ c3> <bb2 d3> g2 gb2").slow(9)
  .sound("sine")
  .gain(0.13)
  .cutoff(perlin.range(400, 3000).slow(12))
  .room(0.27)
  .every(7, x => x.rev())