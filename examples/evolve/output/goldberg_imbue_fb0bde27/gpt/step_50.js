setcps(97/60/4)

$: stack(
  note("g3 gb3 e3 <d3 b2> c3 d3 g2").slow(3)
    .sound("triangle")
    .gain(0.41)
    .lpf(sine.range(740, 3050).slow(11))
    .lpq(6.2)
    .every(5, x => x.rev())
    .sometimes(x => x.juxBy(0.25, y => y.pan(0.85))),
  s("bd*3 <bd bd~>")
    .gain(0.19)
    .room(0.28)
    .cutoff(1320)
    .echo(0.13, 0.19, 0.15)
)

$: s("hh cp <cp [cp~]> hh").slow(3)
  .gain(0.19)
  .pan(perlin.range(0.08,0.89).slow(7))
  .cutoff(3190)
  .hpf(sine.range(680, 1020).slow(13))
  .every(6, x => x.rev())
  .sometimes(x => x.jux(y => y.cutoff(2110)))

$: stack(
  note("<c4 e4 g4 eb4 d4 f4 eb4>").slow(7)
    .sound("supersaw")
    .gain(0.21)
    .pan(sine.range(0.13,0.94).slow(6))
    .cutoff(3380)
    .tremolo(0.42)
    .every(7, x => x.rev()),
  note("a3 <d4~ f4> g3").slow(10)
    .sound("supersaw")
    .gain(0.12)
    .tremolo(0.74)
    .lpf(1230)
)

$: note("g4 d4 [f4~] <e4 a4>").slow(6)
  .sound("piano")
  .gain(0.18)
  .room(0.29)
  .release(1)
  .pan(sine.range(0.21,0.81).slow(6))
  .every(4, x => x.rev())
  .juxBy(0.3, x => x.cutoff(2420))

$: stack(
  note("<eb3~ f3~ c3> <bb2 d3> g2 gb2").slow(8)
    .sound("sine")
    .gain(0.16)
    .cutoff(perlin.range(1440, 2790).slow(10))
    .room(0.33)
    .every(6, x => x.jux(y => y.pan(0.94))),
  note("f2~ c2 g2~ d2 <bb1~ d2>").slow(13)
    .sound("square")
    .gain(0.09)
    .cutoff(1400)
    .room(0.21)
    .every(4, x => x.rev())
)

$: note("c5 [eb4~] <d5 a4> g4").slow(7)
  .sound("pulse")
  .gain(0.13)
  .pan(perlin.range(0.14,0.91).slow(9))
  .lpf(2240)
  .every(7, x => x.jux(y => y.rev()))
  .degradeBy(0.08)