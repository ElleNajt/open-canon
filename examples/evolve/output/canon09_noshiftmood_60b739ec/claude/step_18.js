// BWV 1087 Canon 11 - The Shattered Mirror (Variation: Fragments Reassemble)

// The cathedral has fallen. From its ruins, something stranger emerges.
// Voices that chased now FLEE from each other.
// The proclamation becomes a question. The heartbeat — irregular, human.
// What was chromatic desperation becomes chromatic ACCEPTANCE.

let shatteredVoice = x => x
  .sound("triangle")
  .gain(0.13)
  .attack(0.008)
  .decay(0.8)
  .sustain(0.15)
  .release(2.5)
  .lpf(cosine.range(600, 1800).slow(12))
  .room(0.75)
  .delay(0.5)
  .delaytime(0.375)
  .delayfeedback(0.45)

let phoenixAsh = x => x
  .sound("sine")
  .gain(0.14)
  .attack(0.1)
  .decay(0.7)
  .sustain(0.35)
  .release(1.8)
  .lpf(1600)
  .lpq(3)
  .room(0.7)

setcps(108/60/4)

// Track 1 - Ascending phrase now DESCENDS — acceptance of gravity
$: note("d5 ~ cs5 ~ c5 ~ b4 ~ bb4 ~ a4 ~ gs4 ~ g4 ~")
  .slow(8)
  .apply(shatteredVoice)
  .pan(0.35)
  .juxBy(0.4, x => x.transpose(7).gain(0.07).late(0.75))

// Track 2 - Former falling canon now RISES to meet it — convergence
$: note("g4 ~ gs4 ~ a4 ~ bb4 ~ b4 ~ c5 ~ cs5 ~ d5 ~")
  .slow(8)
  .sound("sine")
  .gain(0.11)
  .attack(0.02)
  .decay(0.5)
  .sustain(0.25)
  .release(1.8)
  .lpf(1400)
  .room(0.65)
  .pan(0.65)
  .late(2)

// Track 3 - Hocket SIMPLIFIES to duet — two voices finding each other
$: note("g5 ~ ~ ~ d5 ~ ~ ~ eb5 ~ ~ ~ g5 ~ ~ ~")
  .slow(4)
  .sound("square")
  .gain(0.08)
  .attack(0.002)
  .decay(0.15)
  .sustain(0.08)
  .release(0.25)
  .lpf(1600)
  .room(0.35)
  .pan(0.3)

$: note("~ ~ bb4 ~ ~ ~ a4 ~ ~ ~ c5 ~ ~ ~ bb4 ~")
  .slow(4)
  .sound("square")
  .gain(0.08)
  .attack(0.002)
  .decay(0.15)
  .sustain(0.08)
  .release(0.25)
  .lpf(1600)
  .room(0.35)
  .pan(0.7)

// Track 4 - Drone UNIFIES — fifths resolve to single tone, then breathe
$: note("g1 ~ ~ ~ ~ ~ ~ ~ g1 ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("sawtooth")
  .gain(0.14)
  .attack(4.0)
  .decay(3.0)
  .sustain(0.5)
  .release(5.0)
  .lpf(sine.range(60, 180).slow(16))
  .lpq(5)
  .pan(0.5)
  .room(0.8)

// Track 5 - DROPPED 7-beat cycle — replaced with GAMELAN BELLS (pentatonic 5s)
$: note("g5 bb5 c6 d6 f6")
  .slow(2.5)
  .sound("triangle")
  .gain(0.07)
  .attack(0.001)
  .decay(1.2)
  .sustain(0.05)
  .release(2.0)
  .lpf(3500)
  .hpf(800)
  .room(0.85)
  .pan(rand.range(0.2, 0.8))
  .delay(0.4)
  .delaytime(0.2)
  .delayfeedback(0.35)

// Track 6 - Proclamation becomes LAMENT — falling, searching
$: note("g4 ~ f4 ~ eb4 ~ d4 ~ ~ ~ eb4 ~ f4 ~ g4 ~ ~ ~")
  .slow(8)
  .apply(phoenixAsh)
  .pan(0.55)
  .juxBy(0.5, x => x.transpose(-12).gain(0.09).late(0.5))

// Track 7 - Walking bass becomes BREATHING bass — long tones, fewer steps
$: note("g2 ~ ~ ~ bb2 ~ ~ ~ c3 ~ ~ ~ d3 ~ ~ ~")
  .slow(8)
  .sound("sawtooth")
  .gain(0.14)
  .attack(0.15)
  .decay(0.4)
  .sustain(0.5)
  .release(0.8)
  .lpf(350)
  .lpq(4)
  .pan(0.5)
  .room(0.4)

// Track 8 - Stuttering memory becomes SINGULAR memory — one note, patient
$: note("d5 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.09)
  .attack(0.01)
  .decay(0.3)
  .sustain(0.15)
  .release(1.5)
  .lpf(1800)
  .delay(0.75)
  .delaytime(0.333)
  .delayfeedback(0.7)
  .pan(0.5)

// Track 9 - Heartbeat becomes IRREGULAR — human, hesitant, alive
$: note("g1 ~ ~ g1 ~ ~ ~ ~ g1 ~ g1 ~ ~ ~ ~ ~")
  .slow(5)
  .sound("sine")
  .gain(0.14)
  .attack(0.015)
  .decay(0.18)
  .sustain(0.06)
  .release(0.2)
  .lpf(85)
  .room(0.25)
  .pan(0.5)

// Track 10 - Chords find RESOLUTION — no more betrayal, gentle acceptance
$: note("<g3 bb3 d4> ~ ~ ~ ~ ~ ~ ~ <g3 b3 d4> ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.12)
  .attack(0.2)
  .decay(1.0)
  .sustain(0.4)
  .release(2.0)
  .lpf(900)
  .lpq(2)
  .room(0.7)
  .pan(0.5)

// Track 11 - NEW: WIND THROUGH RUINS — noise-like breath, spectral
$: note("g6 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ d6 ~ ~ ~")
  .slow(16)
  .sound("sawtooth")
  .gain(0.06)
  .attack(1.5)
  .decay(2.5)
  .sustain(0.2)
  .release(4.0)
  .lpf(5000)
  .hpf(2000)
  .lpq(8)
  .room(0.95)
  .delay(0.7)
  .delaytime(1.0)
  .delayfeedback(0.55)
  .pan(cosine.range(0.2, 0.8).slow(14))

// Track 12 - Declaration becomes QUESTION — rising, uncertain
$: note("d4 ~ ~ ~ f4 ~ ~ ~ g4 ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("triangle")
  .gain(0.1)
  .attack(0.3)
  .decay(0.5)
  .sustain(0.4)
  .release(1.5)
  .lpf(1100)
  .room(0.75)
  .pan(0.45)

// Track 13 - NEW: CHILDREN'S MELODY — innocent, pentatonic, hope from wreckage
$: note("g5 ~ a5 ~ c6 ~ d6 ~ c6 ~ a5 ~ g5 ~ ~ ~")
  .slow(6)
  .sound("sine")
  .gain(0.08)
  .attack(0.005)
  .decay(0.3)
  .sustain(0.2)
  .release(0.8)
  .lpf(2200)
  .room(0.5)
  .pan(0.6)
  .juxBy(0.3, x => x.transpose(12).gain(0.05).late(0.125))