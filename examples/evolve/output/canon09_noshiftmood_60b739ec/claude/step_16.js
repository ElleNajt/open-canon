// BWV 1087 Canon 10 - The Gathering Storm (Variation: The Fracturing Certainty)

// The defiant ember catches wind. What was one voice
// becomes many — but they do not march in step.
// Each fragment of resistance finds its own angular path,
// colliding, diverging, refusing easy resolution.

let condemnedVoice = x => x
  .sound("sine")
  .gain(0.14)
  .attack(0.002)
  .decay(1.2)
  .sustain(0.01)
  .release(3.0)
  .lpf(sine.range(800, 2400).slow(16))
  .room(0.85)
  .delay(0.7)
  .delaytime(0.5)
  .delayfeedback(0.55)

let fracturingVoice = x => x
  .sound("triangle")
  .gain(0.11)
  .attack(0.008)
  .decay(0.4)
  .sustain(0.35)
  .release(0.8)
  .lpf(1900)
  .room(0.55)

setcps(108/60/4)

// Track 1 - The ascending phrase SPLINTERS — tritone intrusions
$: note("g4 ~ a4 ~ ~ bb4 ~ ~ cs5 ~ ~ d5 ~ ~ eb5 ~")
  .slow(8)
  .apply(condemnedVoice)
  .pan(0.3)
  .juxBy(0.5, x => x.transpose(-12).gain(0.08).late(0.5))

$: note("d4 ~ eb4 ~ fs4 ~ ~ g4 ~ ~ a4 ~ ~ bb4 ~")
  .slow(8)
  .apply(condemnedVoice)
  .gain(0.1)
  .pan(0.7)
  .late(0.25)

// Track 2 - Resistance FRAGMENTS into hocket — voices interrupting each other
$: note("g5 ~ ~ ~ ~ ~ g5 ~ ~ ~ ~ ~ bb5 ~ ~ ~")
  .slow(4)
  .sound("square")
  .gain(0.1)
  .attack(0.001)
  .decay(0.12)
  .sustain(0.08)
  .release(0.25)
  .lpf(1600)
  .room(0.35)
  .pan(0.35)

$: note("~ ~ eb5 ~ ~ ~ ~ ~ eb5 ~ ~ ~ ~ ~ c5 ~")
  .slow(4)
  .sound("square")
  .gain(0.1)
  .attack(0.001)
  .decay(0.12)
  .sustain(0.08)
  .release(0.25)
  .lpf(1600)
  .room(0.35)
  .pan(0.65)

// Track 3 - Drone DISSOLVES into cluster — authority crumbling
$: note("<g1 ab1 a1> ~ ~ ~ ~ ~ ~ ~ <fs1 g1 gs1> ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("sawtooth")
  .gain(0.12)
  .attack(3.5)
  .decay(2.0)
  .sustain(0.45)
  .release(4.0)
  .lpf(cosine.range(55, 160).slow(14))
  .lpq(6)
  .pan(0.5)
  .room(0.7)

// Track 4 - Pulse becomes IRREGULAR — the heart stumbles
$: note("d4 ~ a4 ~ ~ d4 ~ a4 d4 ~ ~ a4 e4 ~ ~ b4")
  .slow(4)
  .sound("triangle")
  .gain(0.1)
  .attack(0.005)
  .decay(0.2)
  .sustain(0.3)
  .release(0.4)
  .lpf(1500)
  .room(0.5)
  .pan(0.45)

// Track 5 - Bass ABANDONS pattern — leaps into unpredictable intervals
$: note("g2 ~ ~ db3 ~ ~ a1 ~ ~ eb3 ~ ~ b1 ~ ~ f3")
  .slow(8)
  .sound("sawtooth")
  .gain(0.14)
  .attack(0.03)
  .decay(0.35)
  .sustain(0.35)
  .release(0.7)
  .lpf(380)
  .lpq(4)
  .pan(0.5)
  .room(0.35)

// Track 6 - DROPPED: War cry silenced. In its place — a stuttering memory
$: note("g4*3 ~ ~ ~ d4*2 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.09)
  .attack(0.002)
  .decay(0.15)
  .sustain(0.05)
  .release(0.8)
  .lpf(2200)
  .delay(0.75)
  .delaytime(0.166)
  .delayfeedback(0.6)
  .pan(0.5)

// Track 7 - Heartbeat STOPS and RESTARTS — syncopated silence
$: note("g1 ~ g1 ~ ~ ~ ~ ~ ~ ~ g1 ~ g1 ~ g1 ~")
  .slow(4)
  .sound("sine")
  .gain(0.16)
  .attack(0.01)
  .decay(0.25)
  .sustain(0.1)
  .release(0.2)
  .lpf(85)
  .room(0.25)
  .pan(0.5)

// Track 8 - Major defiance QUESTIONED — alternating major/diminished
$: note("<g3 b3 d4> ~ ~ ~ <c4 eb4 gb4> ~ ~ ~ <d4 fs4 a4> ~ ~ ~ <ab3 c4 eb4> ~ ~ ~")
  .slow(8)
  .sound("sawtooth")
  .gain(0.1)
  .attack(0.08)
  .decay(0.6)
  .sustain(0.35)
  .release(1.0)
  .lpf(750)
  .lpq(5)
  .room(0.6)
  .pan(0.5)

// Track 9 - NEW: THE CONTRARIAN — inverted rebel melody, arguing with itself
$: note("d5 ~ ~ f5 ~ ~ bb4 ~ ~ ~ a4 ~ c5 ~ ~ eb5")
  .slow(6)
  .apply(fracturingVoice)
  .pan(cosine.range(0.3, 0.7).slow(6))
  .juxBy(0.4, x => x.transpose(7).late(0.0625))

// Track 10 - Echo chamber COLLAPSES — shorter, drier, desperate
$: note("g4 d5 g4 a4 d5 bb4 g4 ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.1)
  .attack(0.05)
  .decay(0.3)
  .sustain(0.15)
  .release(1.2)
  .lpf(1400)
  .room(0.5)
  .delay(0.4)
  .delaytime(0.25)
  .delayfeedback(0.35)
  .pan(0.5)

// Track 11 - Declaration becomes QUESTION AGAIN — falling now
$: note("d5 ~ ~ ~ ~ ~ ~ ~ g4 ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("triangle")
  .gain(0.1)
  .attack(0.8)
  .decay(0.5)
  .sustain(0.5)
  .release(2.5)
  .lpf(1100)
  .room(0.85)
  .pan(0.5)

// Track 12 - NEW: METALLIC GHOST — spectral presence watching the fracture
$: note("~ ~ ~ ~ ~ ~ cs6 ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("square")
  .gain(0.06)
  .attack(0.5)
  .decay(1.5)
  .sustain(0.2)
  .release(3.0)
  .lpf(3200)
  .hpf(2800)
  .room(0.9)
  .delay(0.8)
  .delaytime(0.666)
  .delayfeedback(0.65)
  .pan(0.5)