// BWV 1087 Canon 11 - The Shattered Mirror (Variation: Seeds in Ash)

// From acceptance emerges something unexpected: play.
// The ruins become a garden. Chromatic grief transforms to modal wonder.
// Voices no longer chase or flee — they DANCE.
// The heartbeat finds its rhythm. Children's games echo through fallen stones.

let seedlingVoice = x => x
  .sound("triangle")
  .gain(0.14)
  .attack(0.003)
  .decay(0.5)
  .sustain(0.25)
  .release(1.8)
  .lpf(sine.range(800, 2400).slow(10))
  .room(0.65)
  .delay(0.35)
  .delaytime(0.25)
  .delayfeedback(0.4)

let gardenBell = x => x
  .sound("sine")
  .gain(0.12)
  .attack(0.001)
  .decay(0.8)
  .sustain(0.1)
  .release(1.5)
  .lpf(2800)
  .room(0.8)

setcps(126/60/4)

// Track 1 - Descending line now LEAPS — playful, unpredictable intervals
$: note("d5 ~ g4 ~ c5 ~ f4 ~ bb4 ~ eb5 ~ a4 ~ d5 ~")
  .slow(6)
  .apply(seedlingVoice)
  .pan(0.35)
  .juxBy(0.35, x => x.transpose(12).gain(0.06).late(0.5))

// Track 2 - Rising line becomes CALL AND RESPONSE — conversation
$: note("g4 ~ ~ d5 ~ ~ a4 ~ ~ e5 ~ ~ b4 ~ ~ g5")
  .slow(6)
  .sound("sine")
  .gain(0.11)
  .attack(0.015)
  .decay(0.4)
  .sustain(0.3)
  .release(1.5)
  .lpf(1600)
  .room(0.6)
  .pan(0.65)
  .late(1.5)

// Track 3 - Hocket becomes CLAPPING GAME — rhythmic, joyful
$: note("g5 g5 ~ d5 ~ g5 g5 ~ d5 d5 ~ g5 ~ d5 d5 ~")
  .slow(3)
  .sound("square")
  .gain(0.07)
  .attack(0.001)
  .decay(0.08)
  .sustain(0.05)
  .release(0.15)
  .lpf(1800)
  .room(0.3)
  .pan(0.3)

$: note("~ ~ bb4 ~ bb4 ~ ~ a4 ~ ~ c5 c5 ~ ~ a4 ~")
  .slow(3)
  .sound("square")
  .gain(0.07)
  .attack(0.001)
  .decay(0.08)
  .sustain(0.05)
  .release(0.15)
  .lpf(1800)
  .room(0.3)
  .pan(0.7)

// Track 4 - Drone BLOSSOMS into CHORD — life emerging
$: note("<g1 d2 g2> ~ ~ ~ ~ ~ ~ ~ <g1 d2 a2> ~ ~ ~ ~ ~ ~ ~")
  .slow(12)
  .sound("sawtooth")
  .gain(0.12)
  .attack(3.0)
  .decay(2.5)
  .sustain(0.55)
  .release(4.0)
  .lpf(cosine.range(80, 220).slow(12))
  .lpq(4)
  .pan(0.5)
  .room(0.75)

// Track 5 - Gamelan becomes MUSIC BOX — delicate, mechanical wonder
$: note("d6 ~ g5 ~ a5 ~ d6 ~ c6 ~ g5 ~ a5 ~ c6 ~")
  .slow(2)
  .apply(gardenBell)
  .pan(rand.range(0.3, 0.7))
  .delay(0.3)
  .delaytime(0.167)
  .delayfeedback(0.3)

// Track 6 - Lament transforms to LULLABY — tender, circular
$: note("g4 a4 bb4 a4 g4 ~ d4 ~ g4 a4 bb4 c5 d5 ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.13)
  .attack(0.08)
  .decay(0.6)
  .sustain(0.4)
  .release(1.5)
  .lpf(1300)
  .room(0.7)
  .pan(0.55)

// Track 7 - Breathing bass becomes WALKING again — but lighter, PIZZICATO
$: note("g2 ~ d3 ~ bb2 ~ f3 ~ c3 ~ g3 ~ d3 ~ a2 ~")
  .slow(4)
  .sound("triangle")
  .gain(0.14)
  .attack(0.002)
  .decay(0.2)
  .sustain(0.08)
  .release(0.4)
  .lpf(500)
  .lpq(3)
  .pan(0.5)
  .room(0.35)

// Track 8 - Single note becomes BIRDSONG — quick, high, scattered
$: note("d6 ~ ~ g6 ~ ~ e6 ~ ~ ~ a6 ~ ~ ~ f6 ~")
  .slow(4)
  .sound("sine")
  .gain(0.08)
  .attack(0.002)
  .decay(0.15)
  .sustain(0.08)
  .release(0.6)
  .lpf(3500)
  .hpf(1500)
  .delay(0.5)
  .delaytime(0.2)
  .delayfeedback(0.5)
  .pan(rand.range(0.2, 0.8))

// Track 9 - Irregular heart finds STEADY JOY — dance pulse
$: note("g1 ~ g1 ~ g1 ~ g1 ~ g1 ~ ~ g1 g1 ~ g1 ~")
  .slow(4)
  .sound("sine")
  .gain(0.13)
  .attack(0.01)
  .decay(0.12)
  .sustain(0.08)
  .release(0.15)
  .lpf(95)
  .room(0.2)
  .pan(0.5)

// Track 10 - Chords become BRIGHT MAJOR — grief fully transformed
$: note("<g3 b3 d4> ~ ~ ~ <c4 e4 g4> ~ ~ ~ <d4 fs4 a4> ~ ~ ~ <g3 b3 d4> ~ ~ ~")
  .slow(8)
  .sound("sine")
  .gain(0.11)
  .attack(0.15)
  .decay(0.8)
  .sustain(0.45)
  .release(1.8)
  .lpf(1100)
  .lpq(2)
  .room(0.65)
  .pan(0.5)

// Track 11 - Wind becomes WIND CHIMES — tuned, gentle collisions
$: note("b5 ~ ~ d6 ~ ~ fs5 ~ ~ a5 ~ ~ e6 ~ ~ g5")
  .slow(7)
  .sound("triangle")
  .gain(0.07)
  .attack(0.001)
  .decay(1.0)
  .sustain(0.08)
  .release(2.5)
  .lpf(4000)
  .hpf(1200)
  .room(0.9)
  .delay(0.6)
  .delaytime(0.75)
  .delayfeedback(0.45)
  .pan(sine.range(0.25, 0.75).slow(11))

// Track 12 - Question becomes AFFIRMATION — confident, rising to stay
$: note("d4 ~ e4 ~ fs4 ~ g4 ~ a4 ~ b4 ~ d5 ~ ~ ~")
  .slow(6)
  .sound("triangle")
  .gain(0.1)
  .attack(0.2)
  .decay(0.4)
  .sustain(0.45)
  .release(1.2)
  .lpf(1400)
  .room(0.7)
  .pan(0.45)

// Track 13 - Children's melody becomes ROUND — echoing, multiplying joy
$: note("g4 a4 b4 d5 ~ b4 a4 g4 ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(5)
  .sound("sine")
  .gain(0.09)
  .attack(0.004)
  .decay(0.25)
  .sustain(0.25)
  .release(0.7)
  .lpf(2000)
  .room(0.55)
  .pan(0.4)
  .echo(3, 1.25, 0.5)

// Track 14 - NEW: HUMMING DRONE — warm human presence beneath all
$: note("d3 ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~ ~")
  .slow(16)
  .sound("sawtooth")
  .gain(0.09)
  .attack(2.0)
  .decay(4.0)
  .sustain(0.6)
  .release(6.0)
  .lpf(280)
  .lpq(6)
  .room(0.8)
  .pan(0.5)
  .juxBy(0.2, x => x.transpose(0.05).late(0.1))